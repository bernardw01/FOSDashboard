# Harpin FOS Dashboard (Google Workspace Web App)

**PRD version 1.26.0** — `src/Code.js` constant `FOS_PRD_VERSION` and all `src/*` file headers MUST match the version line below.

Product Requirements Document

Version 1.26.0 - 2026-05-13

## 1) Overview

### Purpose

Build a **Google Apps Script**–hosted web application—the **Harpin FOS (Finance & Operations Snapshot) Dashboard**—that gives authorized harpin users a single place to see **key metrics, KPIs, and financial performance** drawn from the systems the company already uses (for example time and labor, agreements and delivery data, and accounting). The dashboard complements point integrations (such as Clockify → Fibery sync) by **aggregating and presenting** cross-system signals rather than replacing those pipelines.

### Product Vision

Provide a **lightweight, Google Workspace–native** experience that:

- authenticates users in the harpin Google Workspace domain and **authorizes** them against a **maintained Google Sheet** (users tab) before showing any dashboard UI,
- loads curated metrics and summaries from configured APIs and/or Google Sheets “metric store” layers,
- presents a clear, scannable layout with optional drill-down where data allows,
- refreshes on a sensible cadence (manual refresh, scheduled server refresh, or both),
- for the **Agreement Dashboard** view (route id `agreement-dashboard`), reads **live Fibery** data through the server (no persistent server-side cache of that payload) while allowing a **browser-side cache** for responsiveness and an explicit **Refresh** action,
- stays maintainable as new data sources and KPIs are added.

### Goals

- **Single pane of glass** for leadership and finance/ops stakeholders on agreed KPIs.
- **No local runtime**—deploy as a published Web App bound to an Apps Script project.
- **Secure by default**: domain restriction, least-privilege service accounts or OAuth patterns as appropriate per connector, secrets in Script Properties or Secret Manager–compatible patterns.
- **Observable operations**: structured logs, last-success timestamps, and clear error surfaces for admins.
- **Extensible architecture**: separate modules for auth shell, data fetch, normalization, and presentation.
- **Brand-aligned UI**: consistent with harpin presentation standards (typography, color, layout patterns established on other internal Web Apps).

### Out of Scope (v1)

- Full **business intelligence** or ad-hoc query builder (no generic “slice any dimension” explorer in v1).
- **Authoritative ledger or ERP replacement**—the dashboard reads from sources of record; it does not become the system of record for financial transactions.
- **Write-back** to external systems from dashboard actions (except explicitly scoped future features).
- **Non–Google Workspace** primary hosting (the product is defined as Apps Script + HtmlService Web App).
- **Mobile-native apps** (responsive web is sufficient for v1).

## 2) Users and Use Cases

### Primary Users

- **Executive and leadership** users who need periodic snapshots of revenue, margin, utilization, cash, or other agreed KPIs.
- **Finance and operations** staff who monitor performance against plan and need trustworthy freshness indicators.
- **Internal admins** who configure data sources, thresholds, and access.

### Core Use Cases

- Open the published dashboard only after the system confirms the user appears on the **authorized users** sheet with a defined **role** and **team**; otherwise see a **not authorized** page.
- Open the published dashboard and see **today’s or period’s** KPI snapshot without hunting across tools.
- **Refresh** metrics on demand and understand **when data was last updated** and from which source.
- Compare **actual vs plan** (or prior period) where those series exist in the metric layer.
- **Troubleshoot** a stale or failed panel using admin-visible logs or status (without exposing secrets).

## 3) Functional Requirements

Each numbered **FR**, **AC** (Acceptance Criteria), and **NFR** (Non-Functional Requirements) item carries a status tag:

| Tag | Meaning |
| --- | --- |
| **[Released]** | Implemented in the current Apps Script project (including Web App deployment behaviors). |
| **[In-Progress]** | Partially implemented or not yet validated end-to-end. |
| **[Backlog]** | Not yet implemented. |

As of **version 1.26.0**, spreadsheet authorization (FR-05–FR-08a + the **per-user Fibery-access gate FR-83** + the **tolerant header / scheme scrub + diagnostic FR-88**), shell navigation affordances (FR-10a–FR-10b), **whole-app branding contract to `agreement-dashboard-prd-v2.md` §9.5–§9.7 (FR-50)**, **Fibery Agreement Management data contract (`## 6)`)**, **the full Agreement Dashboard with live Fibery data, configurable client-side TTL, attention panel, four Chart.js charts, financial performance table with a click-to-open milestones modal (FR-86), Customer Relationship Cards (§7.6), Forward Revenue Pipeline (§7.8), and the D3-rendered Revenue Flow Sankey (§7.11)** (`## 7)`, FR-52–FR-56c + FR-86), **user activity logging (`## 3.8`, FR-60–FR-66)**, **Phase A + Phase B + Phase C of the Utilization Management Dashboard on the Operations panel — paginated Fibery `Agreement Management/Labor Costs` fetch, KPI strip, six Chart.js surfaces, global Customer + Project + Person + Role + Billable + Internal-labor filters with chip bar, click-to-toggle drill on every chart, sortable + paginated Detail Table, configurable client-side TTL, `localStorage`-persisted filter state, the v1.14.0 Phase C **Utilization Alerts panel** (v1.17.0: **alert-kind collapsible grouping** + **Collapse all** affordance, replacing the v1.16.0 by-person axis), **Person × Week heatmap** with heatmap-local Role filter (top 30 contributors), **Pending Approvals widget**, and **row-detail drawer** with the v1.15.0 server-supplied Fibery deep links gated by the `fibery_access` column** (`## 8)`, FR-70–FR-84), and **Phase A + Phase B of the Delivery Dashboard on the Delivery panel — Active Projects table reusing `getAgreementDashboardData()` (no extra Fibery query) with a new client-side substring search input (FR-98), plus a per-project monthly Profit & Loss card lazily fetched via `getDeliveryProjectMonthlyPnL(<id>)` that aggregates `Labor Costs` + `Other Direct Costs` + `Revenue Items` into one row per calendar month spanning the project Duration with margin-bucket coloring keyed to Target Margin, §M.9 reconciliation caption, a per-project `sessionStorage` cache (schema v2), a Table / Chart view toggle (Chart.js stacked bars + Revenue line), a per-month Revenue drill-down modal, projected-month pills + muted chart fills for future months, and a Copy CSV action (FR-94–FR-98)** (`## 9)`, FR-89–FR-98) are documented; acceptance criteria through **AC-59** are tracked; implementation status follows each **[Released]** / **[Backlog]** tag. The user-facing left-nav label and the internal route id are both **Agreement Dashboard** (`agreement-dashboard`); the prior `finance` route id was retired in v1.11.0 — historical activity-log rows tagged `finance` remain queryable alongside new `agreement-dashboard` rows. The Operations nav entry (route id `operations`) flipped from a "coming soon" placeholder to the Utilization Management Dashboard in v1.12.0, and was extended in v1.13.0 with cross-filter drill-down, the Detail Table, and persisted filter state. **As of v1.22.0**, the sidebar nests **Operations** with child entries **Utilization** (`operations`) and **Labor hours** (`labor-hours`); the Labor hours panel (`#panel-labor-hours`) ships an ISO **Mon–Sun** week picker (default = last completed calendar week), weekly hour targets on utilization payloads (`laborHours` from Script Properties), aggregation from the same normalized labor rows as Utilization, a **cache-first** path that avoids `getUtilizationDashboardData` when the existing Utilization client cache date range fully covers the selected week, mirrored **Internal labor** filter state, KPI counts, and sortable Over / Under / On-target tables with a concise projects column (FR-101). **As of v1.24.0**, Labor hours **Phase C + D** adds expandable per-project task splits (`<details>`), **Copy CSV**, print-friendly layout, and dedicated **`labor_hours_*`** activity events (**FR-103** / **AC-59**). **As of v1.25.0**, the **Delivery** area in the sidebar is a **group** with child routes **Projects & P&L** (`delivery`) and **Revenue review** (`revenue-review`); **Revenue review** reuses the Agreement Dashboard **`fos_agreement_dashboard_v2`** payload (cache-first + shared TTL) for executive KPIs, agreement expiry signals, and a future-dated **pre-recognition** callout. **As of v1.26.0**, **Revenue review** adds sortable **milestone billing** (prior + current month), **portfolio**, **revenue by customer**, **overdue**, and **variance** tables, **Copy CSV** (per section + combined), **print** with auto-expanded `<details>`, and a **milestone detail** tree (customer → agreement). **As of v1.13.0, neither the Agreement Dashboard panel nor the Operations panel renders an in-panel harpin logo** — the app sidebar `.fos-brand-logo` is the single rendered brand mark across the entire web app (cosmetic-only cleanup; panel layout otherwise unchanged).

### 3.1 Access, Identity, and Configuration

- FR-01 **[Backlog]**: The Web App MUST restrict access to users in the **harpin Google Workspace** domain (or an explicitly configured allowlist of domains) consistent with Apps Script execution identity and deployment settings, **in addition to** spreadsheet-based authorization (FR-05–FR-08).
- FR-02 **[Backlog]**: The system MUST store non-secret configuration (feature flags, sheet IDs, tab names, API base URLs, **authorization sheet column names**) in **Script Properties** or an equivalent documented store; secrets MUST NOT be committed to source control.
- FR-03 **[Backlog]**: The system MUST fail fast with operator-readable errors when required configuration is missing for a given panel or connector.
- FR-04 **[Backlog]**: The solution SHOULD support a minimal **environment** or **tier** concept (`dev` / `prod`) for separate Script Properties sets if multiple deployments are used.
- FR-05 **[Released]**: The system MUST read **authorized users** from a designated **Google Sheet** tab (default name **`Users`**, overridable via Script Property **`AUTH_USERS_SHEET_NAME`**) within a spreadsheet identified by **`AUTH_SPREADSHEET_ID`** stored in Script Properties.
- FR-06 **[Released]**: The first row of the users tab MUST be a **header row** with stable column names. The system MUST resolve the active user by **email** using a configurable column (default **`Email`**) with **case-insensitive** comparison after **trimming** whitespace.
- FR-07 **[Released]**: When a row matches the active user, the system MUST read **`Role`** and **`Team`** from that row using configurable column names (defaults **`Role`**, **`Team`**). These values MUST drive server-side navigation filtering and future dashboard entitlements; the client MUST NOT be the source of truth for role or team.
- FR-08 **[Released]**: When the active user **does not** appear in the users tab (or email is empty / cannot be resolved under **Execute as: User accessing the web app**), the system MUST respond with a dedicated **not authorized** HtmlService page (no main dashboard shell, no embedded metrics, no `google.script.run` exposure of privileged data for that session beyond the error page itself).
- FR-08a **[Released]**: Every **server function** invokable via `google.script.run` that returns or mutates sensitive data MUST **re-verify** authorization using the same rules as `doGet` (or a shared helper), so bypassing the initial HTML load cannot grant access.

### 3.2 Dashboard Shell and Navigation

- FR-10 **[Backlog]**: The published UI MUST provide a **dashboard shell**: title, optional period selector (e.g. month/quarter), and a consistent content grid for KPI cards and sections.
- FR-10a **[Released]**: Primary **left navigation** entries MUST show a **familiar icon** beside each dashboard label (implemented: **Bootstrap Icons** via CDN, mapped by route id: `home` → Home, `agreement-dashboard` → Agreement Dashboard, `operations` → Utilization (nested under an **Operations** group label as of **v1.22.0**), `labor-hours` → Labor hours (same group), `delivery` → Delivery). Route ids are kebab-case slugs. The legacy route id `finance` (used in v1.5–v1.10.1) was retired in v1.11.0; historical activity-log rows tagged `Route = finance` remain queryable alongside new rows tagged `agreement-dashboard`. The `operations` entry opens the **Utilization Management Dashboard** (`#panel-operations`) as of v1.12.0 — see **`## 8)`** and **FR-70–FR-76**. The `labor-hours` entry opens **`#panel-labor-hours`** per **FR-101**.
- FR-10b **[Released]**: The **bottom of the left sidebar** MUST expose **Settings** with a **gear** icon (replacing the prior **Profile** control), **rendered as a plain navigation-style link** (e.g. an `<a>` with the same `.fos-nav-btn` chrome used by the dashboard entries — no outlined or pill button styling); until the settings surface exists, the control MUST keep the same **coming soon** placeholder behavior as other inactive destinations, and the click handler MUST `preventDefault()` so a placeholder `href="#"` does not push a hash onto the URL.
- FR-11 **[Backlog]**: The shell MUST display **data freshness** per section or globally (e.g. “Last updated: … UTC”) when the server provides that metadata.
- FR-12 **[Backlog]**: Server-invoked actions from the UI (e.g. refresh) MUST use `google.script.run` (or documented successor) with clear loading and completion states.
- FR-13 **[Backlog]**: The layout MUST be **responsive** for desktop-first use; primary content SHOULD be centered with a sensible max width and accessible contrast.

### 3.3 Data Acquisition and Metric Layer

- FR-20 **[Backlog]**: The system MUST support reading **normalized metric definitions** from a documented location (recommended: Google Sheets “metric catalog” tab or structured JSON in Drive) describing id, label, unit, source, and refresh policy.
- FR-21 **[Backlog]**: The system MUST support at least one **server-side** aggregation path: pull from **Google Sheets** tables maintained by existing processes (including ETL or sync jobs such as Clockify → Sheets → Fibery pipelines) without requiring the browser to hold service credentials.
- FR-22 **[Backlog]**: The system SHOULD support additional connectors behind a shared interface (e.g. **Fibery** query, **Clockify** summary, **accounting API**) with per-connector rate limiting and timeouts.
- FR-23 **[Backlog]**: Each connector MUST emit structured results: **success**, **partial** (degraded panel), or **failed** with a safe user message and detailed server log.
- FR-24 **[Backlog]**: Long-running refreshes MUST be **bounded** (chunking, batching, or async continuation patterns) to remain within Apps Script execution limits.
- FR-25 **[Backlog]**: Any **Fibery** read path used for agreement, revenue, or portfolio surfaces MUST conform to **`## 6) Fibery data model requirements (Agreement Management)`** (field paths, databases, and enum semantics). If a workspace diverges, the connector or mapping layer MUST be updated and this PRD’s §6 SHOULD be revised in the same release.

### 3.4 KPI Presentation

- FR-30 **[Backlog]**: The UI MUST render a **KPI card** pattern: label, primary value, optional delta vs comparison period, and optional sparkline or mini-chart when data is available.
- FR-31 **[Backlog]**: Numeric formatting MUST respect **locale-appropriate** grouping and currency/percent units declared in the metric definition.
- FR-32 **[Backlog]**: Panels with missing or failed data MUST show an explicit **empty or error state**, not a misleading zero.
- FR-33 **[Backlog]**: The UI SHOULD support **grouping** (e.g. “Revenue”, “Delivery”, “People & utilization”, “Cash & liquidity”) driven by configuration.

### 3.5 Operations and Observability

- FR-40 **[Backlog]**: The system MUST append or write **structured execution logs** for refresh cycles (timestamp, operation, duration, per-source counts, errors) to a dedicated **log sheet** or consolidated log mechanism documented for admins.
- FR-41 **[Backlog]**: Admins MUST be able to determine **which connector failed** and whether the user-visible dashboard is partial or stale.
- FR-42 **[Backlog]**: Optional **time-driven triggers** SHOULD refresh a cached snapshot on a schedule, with manual refresh still available in the Web App.

### 3.6 Branding and Documentation

- FR-50 **[Released]**: The published HTML UI MUST follow **`agreement-dashboard-prd-v2.md` §9.5 (Design System)**, **§9.6 (Brand Identity)**, and **§9.7 (Layout)** across the **entire** application surface — sidebar, topbar, Home view, modals, not-authorized page, the Agreement Dashboard panel, and the Operations panel — using a single set of root CSS variables (`--bg #061B30`, `--surface #092747`, `--surface2 #0d2e4a`, `--border #1a4060`, `--accent #52C9E5`, `--accent2 #007FA7`, `--accent3 #20B4C4`, `--accent4 #43D6BA`, `--text #FFFEFC`, `--text-muted #A0AEC0`, `--text-dim #2a5a7a`, `--danger #fc5c65`) and the **Inter** typeface at **14px** base. Surface-specific tokens (e.g. the `--ag-*` family inside `.fos-agreement-root`) MUST resolve to those root variables via aliases rather than introduce parallel palettes. The **Agreement Dashboard** and **Operations** surfaces additionally MUST meet **`## 7)`** subsection **7.2** (Agreement Dashboard) and **`## 8)`** subsection **8.2** (Operations) for header / KPI semantics. The **harpin logo** is rendered exactly once per page, in the sidebar (`.fos-brand-logo`); as of v1.13.0 the dashboard panels (`#panel-agreement-dashboard` and `#panel-operations`) MUST NOT carry an in-panel logo or `.agreement-logo-sep` divider — the sidebar is the single brand surface. The dashboard page-heading + subtitle remain inside each panel.
- FR-51 **[Released]**: The Web App SHOULD display the **current PRD version** (this document’s version string) so stakeholders can align feedback with the documented baseline (sidebar footer + not-authorized page; `FOS_PRD_VERSION` in `src/Code.js` must match this document’s version line).

### 3.7 Agreement dashboard (Fibery + client cache)

- FR-52 **[Released]**: The **Agreement Dashboard** (route id `agreement-dashboard`) MUST fetch portfolio data from **Fibery** only through **authorized** `google.script.run` server handlers; it MUST **not** use a **persistent server-side datastore** (Script Properties, Sheet snapshots, or Drive files) whose purpose is to cache Fibery dashboard payloads for serving this view—**Fibery remains authoritative** for that slice. Server queries use Fibery's REST **`/api/commands`** (batched `fibery.entity/query`) via `UrlFetchApp` with credentials read from Script Properties **`FIBERY_HOST`** and **`FIBERY_API_TOKEN`**; neither value is ever returned to the client.
- FR-53 **[Released]**: The client MUST support a **browser-side cache** (e.g. `sessionStorage` or `localStorage`) of the last successful normalized dashboard JSON, keyed by a **schema version**, so repeat views avoid redundant Fibery round-trips until **Refresh** (or the configured TTL elapses, per FR-56b). Cache key: **`fos_agreement_dashboard_v2`**; `cacheSchemaVersion: 2`.
- FR-54 **[Released]**: The agreement dashboard UI MUST expose a visible **Refresh** control that re-invokes the server fetch, updates the client cache on success, and shows **loading**, **error**, and **last refreshed** states derived from server timestamps. As of v1.13.1, the **loading state** MUST be surfaced as a **semi-transparent overlay** (`.fos-loading-overlay`) anchored inside the panel's `.fos-agreement-inner` container — toggled on at the start of every `google.script.run.getAgreementDashboardData` call and off in both the success and failure handlers — so that initial loads, manual refreshes, and background stale-refresh fetches are all clearly distinguishable from rendered data. The overlay covers only the dashboard surface; the sidebar and topbar remain interactive throughout. As of v1.14.2, the spinner inside the overlay is **96×96 px** (doubled from 48×48 px) with an **8 px** border, and both the spinner and its "Loading data…" label use **`position: fixed`** with viewport-centered offsets (`top: calc(50% - 48px); left: calc(50% - 48px)` for the spinner; `top: calc(50% + 64px); left: 50%; transform: translateX(-50%)` for the label) — so the indicator stays at the viewport center regardless of how far the user has scrolled inside the dashboard. The dark / blurred backdrop MUST remain panel-scoped (sidebar + topbar legibility preserved).
- FR-55 **[Released]**: Visual design for the agreement dashboard MUST follow **`## 7) Agreement revenue dashboard`** (subsection **7.2**) and the **branding tokens** in `agreement-dashboard-prd-v2.md` **§9.5–§9.7** and **§9.6** (deep navy surfaces, cyan/teal accents, **Inter**, `logo.svg` at 32px with separator, danger red reserved for errors and negative margin signals).
- FR-56 **[Released]**: Charts, tables, KPI semantics, and color thresholds MUST match **`agreement-dashboard-prd-v2.md` §7–§8** for the full Phase A + Phase B + Phase C surface — KPI strip (§7.2), Attention Items panel (§7.7), Agreement Status Donut (§7.3), Customer Contract Value bar (§7.4), Financial Performance Table (§7.5), Customer Relationship Cards (§7.6), Forward Revenue Pipeline (§7.8), Revenue Recognition Progress stacked bar (§7.9), Agreement Type Mix Donut (§7.10), and Revenue Flow Sankey (§7.11). Implementation uses **Chart.js v4** from the jsDelivr CDN for §7.3 / §7.4 / §7.9 / §7.10, plain HTML/CSS for §7.5 / §7.6 / §7.7 / §7.8, and **D3 v7 + d3-sankey** (jsDelivr CDN) for §7.11 — all chart libraries are lazy-loaded the first time the Agreement Dashboard panel renders.
- FR-56a **[Released]**: §8 thresholds and color maps MUST be defined as code constants with optional **Script Property overrides**: `AGREEMENT_THRESHOLD_LOW_MARGIN` (default 35), `AGREEMENT_THRESHOLD_INTERNAL_LABOR` (default 5000), `AGREEMENT_THRESHOLD_EXPIRY_DAYS` (default 60), `AGREEMENT_TOP_N_RECOGNITION_BARS` (default 10), and `AGREEMENT_INTERNAL_COMPANY_NAMES` (comma-separated). No DB-backed admin store ships in this phase.
- FR-56b **[Released]**: The client cache MUST expose a **configurable TTL** in the Agreement Dashboard panel UI (selector with at minimum **5 min / 10 min / 30 min / Off**, default **10 min**) persisted in `localStorage` under key **`fos_agreement_dashboard_ttl_minutes_v1`**. When the user opens the Agreement Dashboard panel and the cached payload is older than the TTL, the client MUST render the cache immediately **and** trigger a background refresh; a visible **Stale** badge MUST appear in the refresh row whenever the cache is past the TTL. With TTL set to **Off**, no auto-refresh occurs and the stale badge MUST be hidden. The server-side default TTL minutes are exposed via **`getAgreementCacheTtlMinutes()`** (Script Property **`AGREEMENT_CACHE_TTL_MINUTES`**, default 10) so operators can shift the seed value without a client deploy.
- FR-56c **[Released]**: The Revenue Flow Sankey (§7.11) MUST honor two operator-tunable Script Properties: **`AGREEMENT_SANKEY_LINK_OPACITY`** (decimal 0–1, default **0.35**) controls the flow-band fill opacity, and **`AGREEMENT_SANKEY_INCLUDE_INTERNAL`** (boolean, default **false**) controls whether `Internal`-type agreements are included in the diagram. When `false`, internal agreements are filtered out before node/link construction (§7.11.2). The server view model MUST emit `nodes` / `links` with 0-based indices and per-node color (§8.2 for status, §8.5 for customer, §8.3 for type) plus a node-name `(Unassigned)` / `(No Status)` fallback for null relations (§7.11.8).

### 3.8 User Activity Logging

These requirements govern the **page-request activity log** the FOS Dashboard writes for downstream reporting (who used the dashboard, when, and which dashboard view they opened). The store is the **same spreadsheet** as the **Users** tab (FR-05), in a dedicated **`User Activity`** tab. This log is **append-only** and **separate** from the operational logs defined in **FR-40** (connector refresh telemetry); the two SHOULD NOT be merged.

- FR-60 **[Released]**: The system MUST write activity rows to a designated **Google Sheet** tab (default name **`User Activity`**, overridable via Script Property **`AUTH_USER_ACTIVITY_SHEET_NAME`**) inside the spreadsheet identified by **`AUTH_SPREADSHEET_ID`**.
- FR-61 **[Released]**: A logging toggle Script Property **`USER_ACTIVITY_LOGGING_ENABLED`** (default **`true`**) MUST disable all activity writes when set to `false` without throwing errors to clients (graceful no-op).
- FR-62 **[Released]**: Each event row MUST capture, at minimum: **`Timestamp`** (ISO 8601 UTC, server-generated), **`Email`** (resolved server-side from `Session.getActiveUser`), **`Role`** and **`Team`** (snapshot from the Users tab at the moment of the event), **`Event Type`**, **`Route`**, **`Label`** (optional short context), **`Session ID`** (best-effort client token), **`User Agent`** (truncated, best-effort, client-supplied for client-initiated events).
- FR-63 **[Released]**: The supported **`Event Type`** vocabulary in v1 MUST include at minimum: `page_load` (server-recorded on `doGet` for authorized users), `nav_view` (client-recorded on SPA panel switch), `refresh` (client-recorded when a panel-level refresh control is used), and `server_call` (server-recorded for selected privileged handlers). Additional event types MAY be added in later versions without renaming existing ones.
- FR-64 **[Released]**: The server-side logging handler MUST be invokable from `google.script.run` and MUST **re-verify** spreadsheet authorization (same rules as **FR-08a**) before writing any row; events from unauthorized callers MUST NOT be persisted. The handler MUST be **fire-and-forget safe** (failures do not break the user-visible UI).
- FR-65 **[Released]**: Activity writes MUST use a **single batched append per event** under a **script-level lock** with a short wait (recommended ≤ 2 seconds). Because the FOS Dashboard is a **standalone** Apps Script Web App (not container-bound to the Users spreadsheet), the implementation MUST use **`LockService.getScriptLock()`** — **`LockService.getDocumentLock()`** returns `null` for standalone scripts and MUST NOT be used. On lock-acquisition timeout the event MAY be dropped silently, but a warning MUST be logged via `Logger`/`console.warn` for admin visibility.
- FR-66 **[Released]**: The activity log MUST NOT contain **secrets**, raw request payloads, or **PII beyond** what is already present on the Users tab (email, role, team) plus user-agent and route identifiers. Free-text **`Label`** fields MUST be bounded (recommended ≤ 120 chars) and MUST NOT include Fibery tokens, Script Property keys, or any field captured from form inputs.

### 3.9 Utilization Management Dashboard (Fibery + client cache)

These requirements govern the **Operations** route (route id `operations`, panel `#panel-operations`) — the **Utilization Management Dashboard** that surfaces how the harpin AI team spends time across customers and projects, powered by Fibery's `Agreement Management/Labor Costs` table. The full feature spec lives in **`docs/features/005-utilization-management-dashboard.md`**.

- FR-70 **[Released]**: The **Utilization Management Dashboard** (route id `operations`) MUST fetch labor data from **Fibery** only through **authorized** `google.script.run` server handlers; it MUST **not** use a **persistent server-side datastore** (Script Properties, Sheet snapshots, Drive files) whose purpose is to cache utilization payloads for serving this view — **Fibery remains authoritative** for that slice. Server queries use Fibery's REST **`/api/commands`** (`fibery.entity/query` against `Agreement Management/Labor Costs`) via `UrlFetchApp` with credentials read from Script Properties **`FIBERY_HOST`** and **`FIBERY_API_TOKEN`**; neither value is ever returned to the client. The server-side orchestrator is **`getUtilizationDashboardData(rangeStart?, rangeEnd?)`** and re-checks spreadsheet authorization via `requireAuthForApi_()` before issuing any Fibery call.
- FR-71 **[Released]**: The server MUST **paginate** the labor-cost query using `q/limit` + `q/offset`, accumulating pages until a page returns fewer than the limit (or until an internal page ceiling, default 20, is hit — in which case the payload carries a `partial: true` flag and a non-fatal warning so the client can prompt the user to narrow the range). Date filtering uses `Agreement Management/Start Date Time` with the `q/and / >= / <` predicate pair, and `q/order-by` MUST use the wrapped-vector form **`[[['Agreement Management/Start Date Time'], 'q/desc']]`** matching the PRD v1.9.2 fix.
- FR-72 **[Released]**: The server payload MUST include, for the requested date range: **`rows`** (one normalized labor entry per row with coerced `hours`, derived `week` / `day`, boolean `billable` / `isPending` / `isInternal`, and `revenueFromLabor` when `User Role Bill Rate` is known), **`kpis`** (Total Hours, Billable Hours, Utilization %, Total Cost, Effective Cost Rate, Effective Bill Rate + coverage fraction, Pending Approvals count, distinct persons / projects / customers), **`dimensions`** (sorted Customer / Project / Person / Role lists with stable per-row colors), **`aggregates`** (`byCustomer` / `byProject` / `byPerson` / `byRole` / `byWeek` / `billableVsNonBillable`), and **`pendingApprovals`** (capped subset of `rows` where `isPending = true`). Aggregations are server-precomputed for the **unfiltered** view so the first paint is fast; the client re-aggregates in memory when filters change without a server roundtrip.
- FR-73 **[Released]**: The client MUST cache the last successful payload in **`sessionStorage`** under key **`fos_utilization_dashboard_v1`** (`cacheSchemaVersion: 1`). A **date-range change** invalidates the cache and triggers a fresh server fetch; filter / drill changes do **not** touch the cache. The client cache MUST NOT contain secrets (Fibery tokens, Script Property keys) — payloads are normalized JSON only. As of v1.13.1, the loading state MUST be surfaced as a **semi-transparent overlay** (`.fos-loading-overlay`) anchored inside `#panel-operations .fos-agreement-inner` — toggled on at the start of every `google.script.run.getUtilizationDashboardData` call and off in both the success and failure handlers. As of v1.14.2, the spinner inside the overlay is **96×96 px** (doubled) with an **8 px** border, and both the spinner and label use **`position: fixed`** with viewport-centered offsets so the indicator stays at the visible viewport center regardless of scroll position — same contract as FR-54 for the Agreement Dashboard. As of v1.13.1, navigating away from `#panel-operations` and back MUST NOT re-execute `applyUtilPayload()` when the cached payload's `fetchedAt` matches the timestamp the DOM already reflects — the panel-switch handler MUST compare `utilRenderState.lastRenderedFetchedAt` against the cache's `fetchedAt` and skip the re-render (preserving Chart.js / Detail Table state) when they match. The equivalent skip-rerender rule applies to `showAgreementDashboard()` against `agreementRenderState.lastRenderedFetchedAt`.
- FR-74 **[Released]**: The Operations panel MUST expose a **configurable client-side TTL** (selector with at minimum **5 min / 10 min / 30 min / Off**, default **10 min**) persisted in `localStorage` under key **`fos_utilization_dashboard_ttl_minutes_v1`**. When the panel is opened with a cached payload older than the TTL, the client MUST render the cache immediately **and** trigger a background refresh; a visible **Stale** badge MUST appear in the refresh row whenever the cache is past the TTL. With TTL set to **Off**, no auto-refresh occurs and the stale badge MUST be hidden. The server default is exposed via **`getUtilizationCacheTtlMinutes()`** (Script Property **`UTILIZATION_CACHE_TTL_MINUTES`**, default 10).
- FR-75 **[Released]**: The Operations panel MUST expose a **filter bar** with: **Date range** preset (Last 30 / 90 / 6mo / YTD / Custom; default 90 days, matching Script Property **`UTILIZATION_DEFAULT_RANGE_DAYS`**), **Customer multi-select**, **Project multi-select**, **Billable** segmented toggle (`All` / `Billable` / `Non-billable`, default `All`), **Internal labor** segmented toggle (`Include` / `Exclude`, default `Include`), an active-filter chip row with `×` on each chip, and a **Clear filters** button. Filters compose with **AND** across dimensions and **OR** within a multi-select dimension. Toggling **Internal labor → Exclude** removes rows where **`isInternal = true`** (Clockify-User-Company in `UTILIZATION_INTERNAL_COMPANY_NAMES` OR `agreementType = "Internal"`) from every chart and KPI. The Billable and Internal-labor toggles are independent — they describe different facets and may be applied together. Every filter change in this row MUST be activity-logged under event type **`util_filter`** with `Route = operations`.
- FR-76 **[Released]**: The Operations panel MUST render four **Chart.js v4** surfaces in Phase A — **Hours by Customer** (horizontal bar, top-N capped by `UTILIZATION_TOP_N_CUSTOMERS`, default 20), **Hours by Project** (horizontal bar, top-N capped by `UTILIZATION_TOP_N_PROJECTS`, default 20, each bar colored by its customer hue), **Weekly trend** (line chart, two series: Total Hours and Billable Hours, X axis = ISO Monday-anchored week), and **Billable vs Non-billable** (stacked bar per week). Clicking a bar in **Hours by Customer** or **Hours by Project** toggles that value in the corresponding filter set; clicking a segment of **Billable vs Non-billable** toggles the Billable filter. Each drill-click MUST be activity-logged under event type **`util_drill`** with `Route = operations` and a short `dimension=value` label.
- FR-77 **[Released]**: As of v1.13.0 (**Phase B**), the Operations panel MUST additionally expose **Person** and **Role** multi-select dropdowns in the filter bar (sorted by hours desc; the same `populateMultiMenu` pattern as Customer / Project), and MUST render two additional Chart.js v4 surfaces: **Hours by Role** (doughnut chart colored from `UTILIZATION_ROLE_PALETTE_` indexed by sorted-hours order, server-emitted `dimensions.roles[].color`) and **Hours by Person** (horizontal bar, top-N capped by `UTILIZATION_TOP_N_PERSONS`, default 20, each bar colored by the person's primary role). Filter keys MUST match the server-side personKey (`userId || userName || '(Unknown user)'`) and roleName (`userRole || clockifyUserRole || '(No role)'`). Clicking a slice of the Role donut toggles the corresponding Role filter; clicking a bar in Hours by Person toggles the corresponding Person filter. Each click MUST be activity-logged under `util_drill` with `Route = operations`. Active Person and Role filters MUST render as chips in the chip row with `×` removal, and the **Clear filters** button MUST reset Person + Role alongside Customer + Project + Billable + Internal labor (range stays).
- FR-78 **[Released]**: As of v1.13.0 (**Phase B**), the Operations panel MUST render a **Detail Table** below the charts (full-width section) showing the **filtered, sorted, paginated** row-level labor entries with columns: Date · Person · Customer · Project · Role · Hours (right-aligned) · Cost (right-aligned) · Bill rate (right-aligned, displays `—` when null) · Approval (rendered as a colored pill: Approved teal, Pending amber, Unapproved red, Unknown grey). Every column header MUST be a sort affordance (toggling between asc and desc; null/empty values sort to the bottom regardless of direction). Page size = **100 rows/page**; pager exposes Prev / Next buttons + a page-info label (`{start}–{end} of {total} · page {n} of {N}`). The Detail Table MUST always reflect the current filter set with no separate state. Sort and pagination events MUST be activity-logged under event type **`util_detail`** with `Route = operations`.
- FR-79 **[Released]**: As of v1.13.0 (**Phase B**), the Operations panel MUST persist filter state across reloads in **`localStorage`** under key **`fos_utilization_filters_v1`** (`schemaVersion: 1`). The persisted shape includes `customers` / `projects` / `persons` / `roles` (arrays of selected keys) and `billable` / `internalLabor` (segmented-toggle values). The **`range`** is intentionally **NOT** persisted — opening the panel always uses the default 90-day window unless the user changes the date preset. On `showOperations()`, the client MUST read the stored filters before applying the cached or fresh payload, and on every filter mutation MUST write the current state back. Write failures (private mode / quota) MUST be swallowed silently. Schema-version mismatches MUST cause the stored value to be ignored.
- FR-80 **[Released]**: As of v1.14.0 (**Phase C**), the server payload for the Operations panel MUST include an **`alerts`** array (one entry per surfaced rule hit) and a new **`aggregates.byPersonWeek`** array (one entry per `(personKey, week)` pair). Three rule families MUST be evaluated server-side from `src/utilizationAlerts.js`: (1) **Under-utilized** — Warning. Fires when a person's mean weekly utilization% across the **last 3 complete ISO weeks** in the active range is `< thresholds.underPercent` AND the person logged any hours in those weeks (PTO-only persons are excluded). (2) **Over-allocated** — Critical. Fires when a person's weekly utilization% is `> thresholds.overPercent` in **any 2 consecutive complete weeks** in the range; only the first triggering pair per person is emitted to keep the list tight. (3) **Stale approval** — Warning if the row is pending AND `now - startDateTime ≥ UTILIZATION_STALE_APPROVAL_WARN_DAYS` (default 7); escalates to Critical past `UTILIZATION_STALE_APPROVAL_CRIT_DAYS` (default 14). As of v1.14.1, stale-approval cards MUST be **capped at the 20 oldest** individual cards inside the Alerts panel; any remainder MUST be folded into a single **rollup Warning card** (`kind: 'stale_approval_rollup'`) whose body summarises the hidden count and age span and whose click MUST scroll the user to the Pending Approvals widget with **Show all** activated — preserving the full drill path while keeping the Alerts panel scannable. As of v1.14.1, `isPendingApproval_` MUST treat an explicit `Approval = 'approved'` as **never pending** regardless of `Time Entry Status` (the v1.14.0 logic incorrectly flagged Approved rows whose Time Entry Status was empty), MUST treat `Approval ∈ {unapproved, pending}` as pending, and MUST only fall back to `Time Entry Status ∈ {not_submitted, pending}` when the approval field is missing — a blank `Time Entry Status` alone is no longer sufficient. The output MUST be sorted Critical → Warning → Informational and, within Stale-approval, oldest-first inside each severity bucket. Each entry carries `{id, severity, kind, title, body, target}` where `target` is one of `{person}`, `{rowId}`, `{person, rowId}`, or `{}` (rollup) so the client can deep-link by setting filters or opening the row drawer. The cache schema version is **bumped from 1 → 2**; older clients silently drop their cache and refetch. **As of v1.16.0**, the **client** MUST render this server payload as a grouped collapsible list inside the Utilization Alerts panel; alerts without a per-issue target (the `stale_approval_rollup` and the `all_clear` info card) MUST be rendered as standalone flat cards **above** the grouped section so panel-level messages remain unmissable. The server contract is **unchanged** — grouping is a presentation transform on the existing payload. **As of v1.17.0, the grouping axis is the alert `kind`** (Under-utilized · Over-allocated · Stale approvals), revised from the v1.16.0 by-person axis based on operator feedback that managers triage one class of issue at a time. Standalone-kind set MUST be `{stale_approval_rollup, all_clear}`. Group ordering MUST be: worst-severity-first (Critical → Warning → Info), then alert-count desc, then group-label asc for determinism. Within each group, the original Critical → Warning → Info ordering MUST be preserved. The **worst** severity of a group MUST drive its border-left strip color, header severity icon, and severity-class on the wrapper. Default expand state MUST follow the rule: a group is expanded iff (a) it contains at least one Critical alert, OR (b) it contains exactly one alert (collapse is pointless when there's nothing to scan). The user's manual toggles MUST persist across re-renders within the same payload (`utilState.alerts.expanded` keyed by alert `kind` as of v1.17.0; previously `personKey`) and MUST be cleared at the start of every `applyUtilPayload()` so each new payload starts with the smart defaults. Header click + `Enter` / `Space` keyboard activation MUST toggle the group and MUST emit a `util_alert_group_toggle` activity-log event (route = `operations`, label = `kind=<kind> · expand|collapse` — revised in v1.17.0 from `personKey=<key>`). The group children container MUST carry a stable `aria-controls` linkage from the header (`role="button"`, `aria-expanded`, `tabindex="0"`); the wrapper MUST carry `data-kind="<kind>"` so the Collapse all handler can sync expand-state back into `utilState.alerts.expanded` without re-deriving from the DOM-safe slug. Reduced-motion users MUST get a non-animated chevron + header per `prefers-reduced-motion: reduce`. **As of v1.17.0**, the panel MUST also surface a **Collapse all** button in the panel header (right-aligned, reusing the existing `.fos-util-clear-btn` pill chrome) that collapses every kind-based group in one click and emits a `util_alert_collapse_all` activity-log event (route = `operations`, label = `groups=<count-collapsed>`). The button MUST be hidden when no kind-based groups exist (panel-flat with just standalone cards) AND when every group is already collapsed; its visibility MUST be recomputed on every render and on every toggle.
- FR-81 **[Released]**: As of v1.14.0 (**Phase C**; **click behavior revised v1.18.0**), the Operations panel MUST render a **Utilization Heatmap** as an SVG grid of `(person × ISO week)` cells: one row per contributor (sorted by total hours desc, capped at **`UTILIZATION_HEATMAP_TOP_N_PERSONS`**, default 30, separate from the §N.7 Hours-by-person cap) and one column per week in the active range. Cell color follows the §U.10 utilization buckets (Under `< 60%` red, Building `60–85%` amber, Target `85–110%` green, Over `> 110%` orange, No-data `#0d2e4a`). Weeks that overlap a range edge are flagged `partial: true` server-side and their capacity is pro-rated by `(daysInRangeInWeek / 7)` so the bucket coloring stays honest; the cell renders the normal bucket color overlaid with a diagonal-hatch SVG `<pattern>` and the tooltip notes the partial fraction. As of v1.18.0 the cell-click behavior is governed by **FR-85** (open a modal listing the contributing labor entries); the prior pin-Person + change-range drill is removed. The heatmap section MUST host its **own** Role multi-select dropdown (distinct from the global Role filter) that scopes the heatmap rows by role without disturbing the rest of the dashboard; the dropdown is populated from `dimensions.roles` sorted by hours desc; the global **Internal labor** toggle MUST still apply to the heatmap (Exclude removes rows where `isInternal=true` from the per-person re-aggregation). Clicking a cell MUST pin the global Person filter to that person AND change the active range to the clicked week (`weekStartIso`/`weekEndIso`) — which triggers a server re-fetch per FR-73 — and MUST emit a `util_heatmap_drill` activity-log event. A header meta line MUST surface `{N} people · {M} weeks` and (when the cap clips) `· {K} more hidden (cap N)`. A legend below the section heading MUST list the bucket thresholds + the partial-week swatch.
- FR-82 **[Released]**: As of v1.14.0 (**Phase C**), the Operations panel MUST render a **Pending Approvals widget** beside the heatmap that lists `isPending` rows from the active filtered set sorted by `startDateTime` desc, capped at 50 visible rows by default, with a **Show all (N)** affordance that flips to "Show top 50". Each row MUST render a colored age badge (`{N}d` in amber for 7-14 days, red for `≥ 14` days), the person + date title, the customer + project meta, and the hours total; clicking a row MUST open the **row-detail drawer**. The Operations panel MUST also render a **`.fos-util-drawer`** off-canvas right panel (z-index above the modal layer) opened from either the Pending Approvals widget OR a Detail Table row (`data-row-id` is set on every `<tr>`) OR a Utilization Alert whose `target.rowId` is set. The drawer MUST surface the full normalized row (Date · Person · Role · Customer · Project · Task · Hours · Cost · Bill rate · Cost rate · Margin/hr · Billable · Approval · Time-entry status · Created · Approved-at · Agreement · Agreement state · Agreement type · Fibery id). The drawer footer MAY render an **Open in Fibery →** anchor; visibility and URL composition are governed by **FR-83** (per-user access gate) and **FR-84** (server-supplied deep-link template) — the previously hard-coded host (`harpinai.fibery.io`) is no longer used. The drawer MUST close on the close button, the backdrop click, AND the Escape key, and MUST honor `prefers-reduced-motion: reduce` by disabling the slide-in transition. All four interactions (alert click, heatmap cell click, pending row click, drawer open) MUST emit dedicated activity-log event types: `util_alert_click`, `util_heatmap_drill`, `util_pending_drill`, `util_drawer_open` (Route = `operations`).
- FR-83 **[Released]**: As of v1.15.0, the authorized-users sheet (FR-05–FR-08a) MUST support an **optional** column **`fibery_access`** (default header name overridable via Script Property `AUTH_COL_FIBERY_ACCESS`). Recognized truthy values are `TRUE` / `True` / `true`, `yes` / `y`, `1`, and the JavaScript / Sheets boolean `true`; everything else (blank, `FALSE`, `0`, `no`, garbage, or a missing column altogether) MUST resolve to **`false`** — i.e. **deny by default**. `getAuthorizationForActiveUser_()` MUST attach `fiberyAccess: boolean` to the success envelope, `requireAuthForApi_()` MUST preserve it, and `buildNavigationModel_()` MUST surface it on the navigation payload as `fiberyAccess`. The Operations row-detail drawer MUST hide the **Open in Fibery →** anchor (collapsing the surrounding `.fos-util-drawer-footer`) whenever `navState.fiberyAccess !== true`. When the column header is absent the server MUST emit a one-time `console.warn` so operators can diagnose why every user is gated. Clicking the anchor MUST emit a `util_drawer_fibery_click` activity-log event (Route = `operations`, label = `rowId=<id>`).
- FR-84 **[Released]**: As of v1.15.0, the **Open in Fibery →** deep-link URL MUST be composed from a **server-supplied, public-only** template (no API token, no other credential). A new module `src/fiberyDeepLinks.js` exposes `getFiberyDeepLinkConfig_()` returning `{ scheme, host, laborCostPathTemplate }` populated from Script Properties **`FIBERY_PUBLIC_SCHEME`** (default `https`), **`FIBERY_DEEP_LINK_HOST`** (falls back to the existing `FIBERY_HOST` when unset), and **`FIBERY_LABOR_COST_PATH_TEMPLATE`** (default `/Agreement_Management/Labor_Costs/{slug}-{publicId}`). The config block MUST be attached to the navigation payload as `nav.fibery` **only when `auth.fiberyAccess === true`** (defense in depth — gated users never receive the host or template). The client composes the URL via `scheme + '://' + host + template.replace('{slug}', slug).replace('{publicId}', publicId)`, where `slug = row.name.trim().replace(/\s/g, '-')` (each whitespace → `-`, no collapse). Example: a row with `name = "2026-03-20 - Alex Anakin - 0.5 hrs"` and `publicId = "167141"` MUST resolve to `https://harpin-ai.fibery.io/Agreement_Management/Labor_Costs/2026-03-20---Alex-Anakin---0.5-hrs-167141`. The anchor MUST be suppressed when `row.publicId` or `row.name` is missing OR the deep-link config is missing OR `fiberyAccess` is `false`. The previous hard-coded URL `https://harpinai.fibery.io/Agreement-Management/Labor-Cost-{publicId}` was wrong on multiple axes (wrong workspace host, wrong entity-type path, wrong slug rule) and MUST NOT appear anywhere in the codebase as of v1.15.0.
- FR-85 **[Released]**: As of v1.18.0, the Operations panel **Utilization Heatmap** cell click MUST open a Bootstrap modal (`#utilHeatmapCellModal`) that lists every labor entry contributing to the clicked `(person, week)` cell, replacing the prior v1.14.0 / FR-81 drill behavior (pin Person filter + switch range to clicked week + server fetch). The row list MUST be derived **client-side** from `utilState.payload.rows` filtered by (a) all current dashboard filters via `applyFilters()` so the count matches what the heatmap cell shows, AND (b) `personKeyForRow_(r) === entry.personKey AND r.week === entry.week`, sorted by `startDateTime` ascending. The modal MUST render columns Date · Customer · Agreement · Project · Task · Hours · Cost · Approval; the footer MUST surface `<count> entries · <sum> h · <sum> $`. The modal MUST emit a `util_heatmap_cell_click` activity-log event (Route = `operations`, label = `person=<name> · week=<weekIso> · rows=<count>`); the prior `util_heatmap_drill` event MUST no longer fire. The cache schema is **unchanged** — no server contract or storage migration is required.
- FR-86 **[Released]**: As of v1.18.0, the Agreement Dashboard **Financial performance** table rows MUST be clickable and keyboard-activatable (`role="button"`, `tabindex="0"`, Enter / Space activate). Clicking a row MUST open a Bootstrap modal (`#finAgreementModal`) that lists every Revenue Item ("milestone") for that agreement with columns **Milestone · Target amount · Target date · Invoice status**. The modal MUST be seeded from a new server-supplied map **`payload.revenueItemsByAgreement: { [agreementId]: [{ id, name, targetAmount, actualAmount, targetDate, recognized, state }] }`** built by re-keying the existing `historicalRevenueItems` + `futureRevenueItems` arrays (no extra Fibery fetch). Within each agreement the items MUST be sorted by `targetDate` ascending, with null-target-date items pushed to the bottom. The **Invoice status** column MUST render the Revenue Item's `state` (Fibery workflow state); a client-side helper MUST color the pill (paid / invoiced / recognized → green, scheduled / planned → amber, pending → orange, cancelled / void → red, fallback → gray). The modal footer MUST emit `Total target value: <sum>` and clicking a row MUST emit an `agreement_milestones_open` activity-log event (Route = `agreement-dashboard`, label = `agreementId=<id> · items=<count>`). Empty agreements MUST render the friendly empty message "No milestones found for this agreement." instead of a blank table.
- FR-87 **[Released]**: As of v1.18.0, the Operations panel's **Persons** multi-select dropdown MUST render entries in case-insensitive locale-aware **alphabetical** order, regardless of the order the server returns them in `dimensions.persons`. The sort MUST be applied to a clone of the array so the cached payload is not mutated and other consumers (heatmap row ordering, KPI computation) continue to see the server's hours-desc sort. The fallback display name for users with neither `id` nor `name` (`(Unknown user)`) MUST sort consistently. Customers / projects / roles dropdowns are intentionally NOT alpha-sorted in v1.18.0 — those keep the server's hours-desc order because top-contributor-first is a useful triage signal there.
- FR-88 **[Released]**: As of v1.18.0, the **Open in Fibery →** access plumbing MUST be more tolerant of operator typos. (1) `findHeaderIndex_` in `src/authUsersSheet.js` MUST fall back to a **loose match** (lowercase + strip non-alphanumeric) after the strict case-insensitive match fails, so `Fibery Access`, `Fibery_Access`, `fibery-access`, and `fiberyAccess` all resolve to the configured `fibery_access` column. The strict match MUST still win when both pass (so two columns differing only in punctuation don't accidentally collide). (2) `getFiberyDeepLinkConfig_` in `src/fiberyDeepLinks.js` MUST scrub the resolved host the same way `fiberyClient.js` does — `host = host.replace(/^https?:\/\//i, '').replace(/\/+$/, '')` — so an operator pasting `https://harpin-ai.fibery.io/` into `FIBERY_HOST` never produces a double-scheme deep-link URL. (3) The client `getFiberyLaborCostUrl_` MUST log a one-line `[FOS] Open-in-Fibery hidden: <reason>` warning to the browser console whenever it returns the empty string, naming the exact failure branch (`row.publicId missing`, `navState.fiberyAccess !== true`, `navState.fibery missing`, etc.) so an operator can diagnose without re-reading the source. (4) A new editor diagnostic **`_diag_fiberyAccess()`** in `src/fiberyDeepLinks.js` MUST return `{ email, authOk, fiberyAccess, deepLinkConfig, sampleUrl, notes[] }` for the calling user so support requests can be triaged from the Apps Script editor.
- FR-89 **[Released]**: As of v1.19.0, the **Delivery** nav entry (route id `delivery`, panel `#panel-delivery`) MUST open the **Delivery Dashboard** (no longer the generic "coming soon" modal) with a header / refresh row / **Active Projects** table and a below-the-table **Profit & Loss** card. The Active Projects list MUST be sourced from `getDeliveryDashboardData()`, which reuses `getAgreementDashboardData()` server-side (zero extra Fibery queries) and re-projects each agreement into a Delivery row containing `{ id, name, customer, type, state, contractValue, revenueRecognized, revenueOutstanding, completionPct, completionBucket, completionColor, laborCosts, materialsOdc, totalCost, marginPct, targetMarginPct, marginVariance, marginVarianceBucket, marginVarianceColor, durStart, durEnd, executionDate, stateColor, typeColor }`. Active-project semantics: `state != "Closed-Lost"` AND `type != "Internal"`, overridable via Script Properties `DELIVERY_ACTIVE_STATES` (CSV whitelist; empty = default rule) and `DELIVERY_EXCLUDE_INTERNAL` (boolean; default `true`). The default table sort MUST be Contract Value desc; each column header MUST toggle between asc/desc on click (numeric columns default desc on first activation, text columns default asc), nulls sort last regardless of direction, and the active sort state MUST persist across reloads in `localStorage` `fos_delivery_filters_v1` (`schemaVersion: 1`). The client cache MUST live in `sessionStorage` under `fos_delivery_dashboard_v1` (`cacheSchemaVersion: 1`); the TTL preference MUST live in `localStorage` under `fos_delivery_dashboard_ttl_minutes_v1`. The `% Complete` column MUST render a colored progress bar keyed off the §D.11 four-bucket map (Under `< 25%` blue, Building `25–75%` teal, On-track `75–100%` green, Over `> 100%` orange — overridable via `DELIVERY_COMPLETION_UNDER_PCT` / `_BUILDING_PCT` / `_OVER_PCT`); the `Margin` column MUST render a `formatMarginPct` value preceded by a `marginVariance` bucket dot (green when `margin ≥ target`, amber when within `DELIVERY_MARGIN_VARIANCE_AMBER_PTS` points below target, default 5, red otherwise, neutral gray when either value is null).
- FR-90 **[Released]**: As of v1.19.0, clicking a row in the Active Projects table MUST mark that row as **selected** (visible `is-selected` treatment with a left accent strip), persist the selection in `localStorage` `fos_delivery_filters_v1`, and reveal the **Profit & Loss** card below with a KPI strip + monthly P&L grid for that project. Clicking the same row again MUST deselect it (`is-selected` removed, KPI strip cleared, monthly grid hidden, P&L subtitle reverts to "Select a project above to see its monthly P&L."). Row activation MUST be both click and keyboard activatable (`role="button"`, `tabindex="0"`, Enter / Space). The KPI strip MUST be rendered **synchronously** from the cached project row carrying five chips: **Contract value** (caption "Lifetime planned"), **Revenue recognized** (caption "<pct>% complete" or `—`), **Total cost** (rendered parenthesized; caption "Labor + Materials & ODC"), **Gross profit** (recognized minus total cost, rendered parenthesized when negative; caption "Recognized basis"), and **Margin** (with sub-caption "tgt <pct>%" when a Target Margin exists, otherwise "No target"). Negative Gross profit and negative Margin MUST add an `is-negative` modifier class that paints the chip's `value` in the danger color. Selection MUST emit a `delivery_project_select` activity-log event (Route = `delivery`, label = `agreementId=<id> · name=<truncated-to-80>`); deselection MUST emit `delivery_project_deselect`.
- FR-91 **[Released]**: As of v1.19.0, the **Monthly P&L** grid MUST be fetched lazily through `getDeliveryProjectMonthlyPnL(agreementId)` and cached client-side under `sessionStorage` key `fos_delivery_pnl_<agreementId>_v1` (`cacheSchemaVersion: 1`). The server endpoint MUST issue **three** small per-agreement Fibery queries — `Agreement Management/Labor Costs` (full lifetime, paginated, capped at `DELIVERY_PNL_MAX_LABOR_ROWS` rows / default 10000; `partial: true` MUST be set when the cap is reached), `Agreement Management/Other Direct Costs` (full lifetime, filtered to `Status = Actual` unless `DELIVERY_PNL_INCLUDE_PROJECTED_ODC = true`), and `Agreement Management/Revenue Item` (filtered to `Revenue Recognized = true`) — and aggregate them into one row per calendar month spanning the project's `Duration`. The month window MUST be `[max(durStart, earliest-activity), min(today, durEnd)]`; months with activity **outside** that window MUST still be emitted with `outOfRange: true` and rendered with an italic `(OOR)` marker on the month label (per decision M.3). Zero-activity months **within** the Duration window MUST be rendered as muted zero rows (per decision M.4). Each month row MUST carry `{ key (yyyy-mm), label ("MMM YYYY"), revenue, labor, expenses, totalCost, grossProfit, marginPct, marginBucket, marginColor, outOfRange, hasActivity }`. Revenue MUST bucket by `Actual Date` if populated, falling back to `Target Date` (decision M.1), and use `Actual Amount` if populated, falling back to `Target Amount`. Labor MUST bucket by `Start Date Time` and sum `Cost`. Expenses MUST bucket by `Other Direct Costs.Date` and sum `Amount`. The grid MUST display columns Month · Revenue · Labor · Expenses · Total cost · Margin $ · Margin %, with Labor / Expenses / Total cost rendered parenthesized as positive numbers (ledger convention) and Margin $ rendered parenthesized when negative. The Margin % cell MUST receive the §D.10 bucket-coloring (`green` / `amber` / `red` / `neutral`) keyed off the agreement's Target Margin via `marginVarianceBucket_(monthMargin, targetMargin, ...)` (per decision M.7).
- FR-92 **[Released]**: As of v1.19.0, the Delivery Dashboard MUST surface three operational signals on the Monthly P&L card. (1) A **Partial data** badge above the grid when `payload.partial === true`, with a tooltip noting the configured Labor Cost row cap and the actual rows read (per decision M.6). (2) A **Lifetime total** row in the grid's `<tfoot>` aggregating the visible months' Revenue / Labor / Expenses / Total cost / Gross profit / Margin % (Margin % is derived from the summed monthlies — `(summedRevenue - summedTotalCost) / summedRevenue × 100` — not pulled from the agreement field, per decision M.7's clarification that the agreement's lifetime Margin is reserved for the KPI strip). (3) A **§M.9 reconciliation caption** under the grid when the summed monthlies diverge from the agreement's lifetime fields by more than **5%** (decision M.5) — the caption MUST list each diverging metric with its summed value, the agreement's lifetime value, and the percent delta (or points delta for Margin %). Below the 5% threshold the caption MUST remain hidden — small drift from rounding / Fibery formula evaluation is expected and intentionally silent. Server errors during the P&L fetch MUST surface in `#delivery-pnl-error` (red, role="alert") and MUST NOT clear the KPI strip — operators retain the synchronously-rendered lifetime view even when the monthly fetch fails.
- FR-93 **[Released]**: As of v1.19.0, the Delivery Dashboard MUST emit **eight** activity-log event types via `logActivity_`, all with Route = `delivery`: `delivery_panel_open` (no label), `delivery_refresh` (label `manual=true` on user-initiated refresh; the Refresh button MUST also invalidate every `fos_delivery_pnl_*` sessionStorage entry so the next selection re-fetches), `delivery_project_select` (label `agreementId=<id> · name=<truncated>`), `delivery_project_deselect` (label `agreementId=<id>`), `delivery_table_sort` (label `column=<col> · direction=<asc|desc>`), `delivery_pnl_fetch_start` (label `agreementId=<id>`), `delivery_pnl_fetch_done` (label `agreementId=<id> · monthCount=<n> · ms=<elapsed>`), `delivery_pnl_fetch_error` (label `agreementId=<id> · code=<reason>`). Logging failures MUST be swallowed silently and MUST NEVER block UI. The standard 250 ms event-throttle window (FR-65) applies — quick double-clicks on a row MUST NOT emit two select events. A new editor diagnostic **`_diag_sampleDeliveryPayload()`** and **`_diag_sampleMonthlyPnL(agreementId)`** in `src/deliveryDashboard.js` MUST return a 1-project / 1-agreement sample of the payload + the §M.9 discrepancy block for support triage.
- FR-94 **[Released]**: As of **v1.20.0**, the Delivery Dashboard's per-project P&L MUST support **projected months** alongside actuals. `getDeliveryProjectMonthlyPnL(agreementId)` MUST (1) drop the `Revenue Recognized = true` filter and return **all** Revenue Items for the agreement, carrying the `recognized` flag and workflow `state` per row, (2) default `DELIVERY_PNL_INCLUDE_PROJECTED_ODC` to **true** (operators MAY opt out by setting it `false`) so `Status = Projected` ODC rows participate in the monthly aggregation, and (3) emit `projected: bool` per month — `true` when the month key is later than the current UTC month — and bump `DELIVERY_PNL_CACHE_SCHEMA_VERSION_` to **2** (the client cache key suffix MUST move from `_v1` to `_v2` so v1.19.0 caches are invalidated cleanly on upgrade). The client Monthly P&L grid MUST render a `Projected` pill next to each month label where `projected === true`, MUST tag the row with the `is-projected` CSS class (muted text + accent-stripe left border), and MUST include `Projected` and `Out of range` columns in the Copy CSV output (FR-96). The Lifetime total row continues to aggregate every month, so it now reflects both recognized actuals and projected forward-revenue when projected months exist.
- FR-95 **[Released]**: As of **v1.20.0**, every month row's **Revenue cell** in the Monthly P&L grid MUST be a drill-down hot-spot when `month.revenueItems.length > 0`. The server endpoint MUST include `revenueItems[]` per month — `[{ id, name, amount, recognized, targetDate, actualDate, state }]` — so the drill-down requires zero additional Fibery fetches. Clicking the Revenue cell (or pressing Enter / Space on the focused cell) MUST open a Bootstrap modal `#deliveryRevenueMonthModal` whose title is `Revenue items — <Month Label>`, subtitle is `<project name> · <N> milestone[s]` (suffixed with ` · Projected month` when the month is projected), and body is a table of `Milestone · Amount · Date · Status` rows with the existing `milestoneStatusColor_` palette. The footer MUST summarize `Total $X · recognized $Y`. The same modal MUST be reachable from the Chart view (FR-94) by clicking a month's bar/data-point. Activity event `delivery_pnl_month_drilldown` MUST fire with Route `delivery` and label `monthKey=<key> · itemCount=<n>`.
- FR-96 **[Released]**: As of **v1.20.0**, the Delivery Dashboard's P&L card MUST include a **Chart / Table** view toggle and a **Copy CSV** action above the grid. The default view MUST be `table`; the user's selection MUST persist in `fos_delivery_filters_v1` alongside sort + selectedId + searchTerm and MUST emit `delivery_pnl_view_toggle` activity events (`view=<table|chart>`). The Chart view MUST render a Chart.js mixed chart (bar dataset stack `Labor` + `Expenses`; line dataset overlay for `Revenue`) re-using the lazy `loadChartJs()` helper. Projected months in the chart MUST render with **muted fill colors** (`rgba(252,92,101,0.32)` Labor, `rgba(249,199,79,0.38)` Expenses, `rgba(67,214,186,0.55)` Revenue point) versus actual months (full-tint). The chart's `onClick` handler MUST open the FR-95 drill-down modal for the clicked month. Copy CSV MUST serialize the visible months + Lifetime total row to a comma-separated payload (columns: Month, Revenue, Labor, Expenses, Total cost, Gross profit, Margin %, Projected, Out of range) and write it to the clipboard via `navigator.clipboard.writeText` with a textarea + `execCommand('copy')` fallback for non-secure / older contexts; success / failure status MUST flash in `#delivery-pnl-csv-status` for ~3 s and emit `delivery_pnl_copy_csv` (label `agreementId=<id> · rows=<n>`).
- FR-97 **[Released]**: As of **v1.20.0**, the chart and table views MUST stay in lock-step. Switching views MUST NOT re-fetch (the server payload is already cached client-side per FR-91). Selecting a new project MUST destroy the prior Chart.js instance (`deliveryState.pnlChart.destroy()`) and rebuild the chart from the fresh payload on the next switch into Chart view. Refreshing the dashboard MUST destroy the chart, clear the per-project P&L cache (already required by FR-91), and reset `deliveryState.lastPnlPayload` so the toolbar collapses back to its placeholder state until the next selection. The Chart.js bundle MUST stay lazy-loaded — opening the Delivery panel and clicking through projects in Table view MUST NOT pull `Chart.min.js`; the bundle is requested only on the user's first Chart-view activation.
- FR-98 **[Released]**: As of **v1.20.0**, the Active Projects table header MUST include a **client-side search input** (`#delivery-search-input`, `<input type="search">`) that filters the visible rows by case-insensitive whitespace-tokenized substring match across Project name + Customer name. The input MUST persist its value across reloads in `fos_delivery_filters_v1` alongside sort + selection + view, MUST debounce the persistence + activity log write by 400 ms, MUST surface an inline `×` clear button when non-empty, and MUST show a dedicated "No projects match the current filter." placeholder distinct from the cache-empty placeholder. Filtering MUST NOT alter `deliveryState.projectsByIndex` — selecting a filter-hidden project via prior persistence MUST still resolve, and the filter MUST NOT clear an existing P&L card render. Activity event `delivery_table_search` MUST fire with label `len=<trimmedLength>` only when the trimmed term is non-empty.
- FR-99 **[Released]**: As of **v1.21.0**, the Delivery Dashboard MUST ship **Phase C client surfaces** without new bulk Fibery queries: (1) a **pacing strip** on the P&L card (below the KPI strip) when the monthly grid is loaded and the project has a parsable `durStart` / `durEnd` window — it compares **timeline progress** (elapsed calendar share of the duration) vs **recognition progress** (lifetime recognized revenue ÷ contract value), shows **linear expected recognition** (contract value × elapsed share), **Δ vs linear**, and a **trailing 3-month average** of recognized revenue from the last up to three non-projected months in the cached monthly payload (tooltips on each track); (2) a **Delivery signals** section above the Active Projects table built only from the **visible** cached project rows (same filter as the table) listing portfolio-level warnings (negative margin, cost pressure vs recognized, completion lag vs midpoint of duration, low recognition near `durEnd`) using `.fos-attention-item` styling — clicking an item MUST select that project, load P&L, scroll the table row into view, emit `delivery_attention_click` (`agreementId=<id> · signal=<id>`), and still emit `delivery_project_select` with `source=attention` in the label alongside `agreementId` + truncated name; (3) a **Portfolio margin-flow Sankey** between the signals section and the Active Projects card aggregating recognized revenue → labor, materials & ODC, and implied gross margin across the **visible** non-Internal rows, reusing the same **D3 v7 + d3-sankey** lazy loader as the Agreement Dashboard with a distinct SVG gradient id prefix so the two diagrams never clash — when aggregate recognized revenue is less than aggregate labor + ODC, flows MUST be split in proportion to costs and a muted caption MUST explain the scaling.
- FR-100 **[Released]**: As of **v1.21.0**, `src/agreementAlerts.js` `evaluateAlerts_()` MUST evaluate three additional **delivery-risk** heuristics before the §6.7 all-clear card (each emits a distinct `id` prefix so sorting and de-duplication stay stable): **`pace-behind:`** — Warning when an agreement is **Delivery In Progress**, non-Internal, has `plannedRev`, `durStart`, and `durEnd`, at least **20%** through the calendar duration window, and recognized revenue is **below 65%** of the straight-line expected value with materiality floor on expected recognition; **`cost-exceeds-rec:`** — Critical when non-Internal recognized revenue is at least **$8K** and labor + materials & ODC exceeds recognized by **>28%**; **`low-rec-near-end:`** — Warning when non-Internal, **Delivery In Progress**, `durEnd` is within **55 days**, `plannedRev > 0`, and the recognition ratio `revRec / plannedRev` is **<35%**.
- FR-101 **[Released]**: As of **v1.22.0**, the Web App MUST expose **Labor hours** as a second Operations-area dashboard (route id `labor-hours`, panel `#panel-labor-hours`) alongside **Utilization** (`operations`). The Labor hours surface MUST default the date control to the **last completed Mon–Sun** calendar week (local), MUST classify each person-week into **Over** / **Under** / **On target** using weekly targets supplied on `getUtilizationDashboardData` responses as `laborHours` (defaults overridable via Script Properties per `src/utilizationThresholds.js`), MUST honor the same **Internal labor** include/exclude filter semantics as the Utilization panel (shared `utilState.filters.internalLabor` + persisted `fos_utilization_filters_v1`), MUST prefer a **client-side slice** of rows from the existing Utilization browser cache when that cache's `range.start` / `range.end` fully covers the selected week (no `google.script.run.getUtilizationDashboardData` call on that path), and MUST fall back to `getUtilizationDashboardData(weekStartIso, weekEndIso)` when the cache does not cover the week. The UI MUST reuse Operations dark chrome (`.fos-agreement-root`, `.fos-section-card`, `.fos-agreement-kpi`, `.fos-loading-overlay`, `.fos-util-detail-table`). **As of v1.23.0 (Labor hours Phase B)** the panel MUST add a **Zero hours** KPI plus a chip list of people in `dimensions.persons` for the loaded payload who have **fewer than ε** hours in the selected week after filters, MUST support optional **`LABOR_HOURS_COMPANY_TARGETS_JSON`** (exact company → weekly hours) and **`LABOR_HOURS_EXCLUDED_PERSON_SUBSTRINGS`** on `laborHours`, and MUST make KPI tiles **scroll targets** (click / Enter / Space) to the Over / Under / On / Zero sections.
- FR-102 **[Released]**: As of **v1.23.0**, `getLaborHoursConfig_()` MUST parse optional Script Properties **`LABOR_HOURS_COMPANY_TARGETS_JSON`** (JSON object of company name → positive hours; case-insensitive name match on `clockifyUserCompany` wins over partner substring / default) and **`LABOR_HOURS_EXCLUDED_PERSON_SUBSTRINGS`** (CSV tokens; case-insensitive substring match on `userName` excludes a person from Labor hours tables and the zero-hours chip list). The client MUST surface these fields on the `laborHours` object returned by `getUtilizationDashboardData`.
- FR-103 **[Released]**: As of **v1.24.0** (**Labor hours Phase C + D**), `#panel-labor-hours` MUST enrich the **Projects** table column with per-person **project** and **task** hour splits rendered as capped native `<details>` / `<summary>` controls (sorted by weight; **+N more** truncation when lists exceed shipped caps), MUST ship **Expand projects** / **Collapse projects** affordances, MUST offer **Copy CSV** that serializes the visible Over / Under / On tables plus the Zero roster to the clipboard, MUST apply **print** CSS that hides shell chrome and cooperates with `beforeprint` / `afterprint` so project bodies print expanded, and MUST emit the activity `eventType` strings **`labor_hours_week_change`**, **`labor_hours_refresh`**, **`labor_hours_export`**, **`labor_hours_kpi_nav`**, and **`labor_hours_sort`** (all with Route `labor_hours`) as enumerated in **AC-59** and whitelisted server-side in `src/userActivityLog.js`.

## 4) Non-Functional Requirements

- NFR-01 (Security) **[Backlog]**: Secrets and tokens MUST never be logged or exposed to the client; use server-side only storage and redacted error messages for end users.
- NFR-02 (Privacy) **[Backlog]**: The dashboard MUST only surface data appropriate for the authenticated audience; **role** and **team** from the users sheet SHOULD inform which metrics or sections appear as the model matures. Row-level rules SHOULD be documented when multiple roles exist.
- NFR-03 (Reliability) **[Backlog]**: Partial connector failures MUST NOT blank the entire dashboard unless the failure is catastrophic; unaffected panels remain usable.
- NFR-04 (Performance) **[Backlog]**: Initial page load SHOULD complete within a target budget (documented per deployment) using caching, parallel fetches where safe, and minimal payload sizes.
- NFR-05 (Maintainability) **[Backlog]**: Codebase MUST separate **UI**, **orchestration**, **connectors**, and **metric mapping** into testable modules following the same discipline as other harpin Apps Script projects.
- NFR-06 (Auditability) **[Backlog]**: Figures shown in the UI SHOULD be traceable to source rows or API responses via documented mapping (for finance review).
- NFR-07 (Client cache) **[Backlog]**: Browser-side caches of dashboard JSON MUST NOT contain **secrets** (Fibery tokens, Script Property keys, service account material); payloads MUST be limited to data already safe for the signed-in viewer.
- NFR-08 (Activity-log privacy) **[Backlog]**: The **User Activity** tab MUST be treated as PII (it ties **email** to **timestamped usage**). Access to the underlying spreadsheet MUST be restricted to admins/operators, and the dashboard UI MUST NOT expose raw activity rows to non-admin users. Retention SHOULD be documented per harpin data policy (no v1 default beyond what the underlying sheet retains).

## 5) Target Architecture (Apps Script)

```text
User (Workspace) → Published Web App (HtmlService)
  -> doGet: Session identity → Users sheet lookup (email → role, team)
       -> if unauthorized: NotAuthorized.html  (no activity row written)
       -> if authorized: DashboardShell.html + server context (role, team)
                         + append `page_load` row → User Activity tab
  -> google.script.run → (each handler re-checks authorization)
       -> Dashboard Orchestrator
       -> Metric catalog (Sheets / Drive JSON)
       -> Connector: Google Sheets metric store
       -> Connector(s): REST APIs (Fibery, Clockify, accounting, …)
       -> Normalize + validate → View models
       -> Optional: write snapshot cache (Script Properties / Sheet tab) for non–Agreement-Dashboard panels only when adopted
       -> Structured logs (admin sheet or Stackdriver-style logging if adopted)
       -> logUserActivity(event): append `nav_view` / `refresh` / `server_call`
                                  row → User Activity tab (LockService append)
  <- JSON view models → Client render (KPI cards, sections, freshness)
       -> Agreement Dashboard route (`agreement-dashboard`): optional sessionStorage/localStorage cache of last Fibery JSON (no secrets); Refresh re-fetches from server
       -> SPA panel switches + Refresh → google.script.run.logUserActivity()
```

Optional **scheduled trigger** path: time-driven run → refresh **allowed** snapshot caches (excluding the Agreement Dashboard Fibery payload cache if absent by policy) → log row, without requiring a user session. The **User Activity** tab is **only** written for end-user-initiated events (no synthetic scheduled rows).

## 6) Fibery data model requirements (Agreement Management)

This section incorporates **Section 3 — Fibery Data Model Requirements** from `docs/agreement-dashboard-prd-v2.md` (**version 2.5**, March 2026). It is the **normative schema contract** for FOS features that consume **Agreement Management** data from Fibery (direct API, MCP, or Sheets/materialized views derived from the same fields). If field names or structures differ in a given workspace, **connectors and queries MUST be updated** to match; the business meanings and classifications below remain the reference for harpin.

### 6.1 Required databases

| Database       | Space                | Purpose                                |
| -------------- | -------------------- | -------------------------------------- |
| `Agreements`   | Agreement Management | Core entity for all contract records   |
| `Companies`    | Agreement Management | Customer and client records            |
| `Revenue Item` | Agreement Management | Milestone-based billing records        |
| `Labor Costs`  | Agreement Management | Time-tracked labor cost entries        |
| `Contacts`     | Agreement Management | Customer contacts linked to agreements |

### 6.2 Agreements — required fields

| Field Name            | Fibery Field Path                                             | Type             | Notes                                                  |
| --------------------- | ------------------------------------------------------------- | ---------------- | ------------------------------------------------------ |
| Name                  | `Agreement Management/Name`                                   | Text             | Agreement display name                                 |
| Workflow State        | `workflow/state` → `enum/name`                                | Enum             | See §6.5 for expected values                           |
| Agreement Type        | `Agreement Management/Agreement Type` → `enum/name`         | Enum             | See §6.6                                               |
| Agreement Progress    | `Agreement Management/Agreement Progress` → `enum/name`       | Enum             | See §6.7                                               |
| Customer              | `Agreement Management/Customer` → `Agreement Management/Name` | Relation         | Links to Companies                                     |
| Total Planned Revenue | `Agreement Management/Total Planned Revenue`                  | Number (formula) | Sum of all linked Revenue Item target amounts          |
| Rev Recognized        | `Agreement Management/Rev Recognized`                         | Number (formula) | Sum of Revenue Items where `Revenue Recognized = true` |
| Total Labor Costs     | `Agreement Management/Total Labor Costs`                      | Number (formula) | Sum of linked Labor Cost entries                       |
| Total Materials & ODC | `Agreement Management/Total Materials & ODC`                  | Number (formula) | Other direct costs                                     |
| Current Margin        | `Agreement Management/Current Margin`                         | Number (formula) | Computed per Fibery (operational definition in agreement PRD §5.1) |
| Target Margin         | `Agreement Management/Target Margin`                          | Number           | Agreed target margin at scoping                        |
| Duration              | `Agreement Management/Duration`                               | Date Range       | Agreement start and end dates                          |
| Execution Date        | `Agreement Management/Execution Date`                         | Date             | Contract signing date                                  |
| Clockify Project ID   | `Agreement Management/Clockify Project ID`                    | Text             | External time-tracking reference                       |

### 6.3 Companies — required fields

| Field Name                    | Fibery Field Path                                    | Type             | Notes                                               |
| ----------------------------- | ---------------------------------------------------- | ---------------- | --------------------------------------------------- |
| Name                          | `Agreement Management/Name`                          | Text             | Company display name                                |
| Funnel Stage                  | `Agreement Management/Funnel Stage` → `enum/name`    | Enum             | See §6.8                                            |
| Segment                       | `Agreement Management/Segment` → `enum/name`         | Enum             | Industry/vertical                                   |
| Lead Source                   | `Agreement Management/Lead Source` → `enum/name`     | Enum             | Origin of the relationship                          |
| Total Customer Contract Value | `Agreement Management/Total Customer Contract Value` | Number (formula) | Sum of planned revenue across all linked agreements |
| NDA Completed                 | `Agreement Management/NDA Completed`                 | Boolean          | Whether NDA is in place                             |

### 6.4 Revenue Item — required fields

| Field Name         | Fibery Field Path                                              | Type     | Notes                               |
| ------------------ | -------------------------------------------------------------- | -------- | ----------------------------------- |
| Name               | `Agreement Management/Name`                                  | Text     | Milestone name                      |
| Target Amount      | `Agreement Management/Target Amount`                           | Number   | Planned billing amount              |
| Actual Amount      | `Agreement Management/Actual Amount`                           | Number   | Amount actually invoiced            |
| Target Date        | `Agreement Management/Target Date`                             | Date     | Planned billing date                |
| Revenue Recognized | `Agreement Management/Revenue Recognized`                      | Boolean  | Whether revenue has been recognized |
| Workflow State     | `workflow/state` → `enum/name`                               | Enum     | Billing status                      |
| Agreement          | `Agreement Management/Agreement` → `Agreement Management/Name` | Relation | Parent agreement                    |
| Customer           | `Agreement Management/Customer` → `Agreement Management/Name`  | Relation | Customer (via agreement)            |

### 6.5 Agreement workflow states

The following workflow states are expected. Additional states may exist; FOS views MUST handle at minimum the list below (including treating **Closed-Lost** as inactive for default active/portfolio views per agreement PRD query rules).

| State                       | Dashboard classification             |
| --------------------------- | ------------------------------------ |
| Identified Opportunity      | Pre-delivery / Pipeline              |
| First Client Call Completed | Pre-delivery / Pipeline              |
| Proposal Delivered          | Proposal                             |
| Closed-Won                  | Pre-delivery                         |
| Delivery In Progress        | Active                               |
| Contract Complete           | Complete                             |
| Closed-Lost                 | Inactive (exclude from active views) |

### 6.6 Agreement types

| Value        | Display                                           |
| ------------ | ------------------------------------------------- |
| Subscription | Recurring / subscription revenue                  |
| Services     | Project-based professional services               |
| License      | Software license                                  |
| Internal     | Internal cost-tracking only — no external revenue |

### 6.7 Agreement progress values

| Value       | Meaning                    |
| ----------- | -------------------------- |
| Not Started | Delivery not yet begun     |
| In Progress | Active delivery underway   |
| Delayed     | Delivery behind schedule   |
| Closing     | Final stages               |
| Complete    | All deliverables fulfilled |

### 6.8 Company funnel stages

| Value    | Meaning                           |
| -------- | --------------------------------- |
| Lead     | Early-stage, no formal engagement |
| Prospect | Active sales pursuit              |
| Customer | At least one completed agreement  |
| Client   | Ongoing active engagement         |

**Further detail** (Fibery query text, computed metrics, alerts, UI components): `docs/agreement-dashboard-prd-v2.md` §4 onward; FOS adoption is tracked in **`## 7)`** below and in **`docs/features/003-agreement-dashboard-fibery-client-cache.md`**.

## 7) Agreement revenue dashboard (Fibery live data + client cache + visual contract)

This section defines how the **Agreement Dashboard** (agreement / revenue) view — exposed as the left-nav entry **Agreement Dashboard**, route id `agreement-dashboard`, panel `#panel-agreement-dashboard` — behaves in the **Google Apps Script** Web App. It **imports** product intent from `docs/agreement-dashboard-prd-v2.md` (**§4–§8** for queries, logic, alerts, components, and color/threshold configuration; **§9.5–§9.7** and **§9.6** for branding and layout) and **does not** require the agreement PRD’s **React/npm static deploy**, **Fly.io**, **Resend**, **MCP-only** transport, or **persisted admin threshold databases**—see **`docs/features/003-agreement-dashboard-fibery-client-cache.md`** for the explicit in/out matrix.

### 7.1 Data and caching rules

- **Fibery** is the **source of truth** for agreement portfolio data for this view.
- The **server** runs Fibery reads through **`UrlFetchApp`** against the REST **`/api/commands`** endpoint (batched `fibery.entity/query` per §4 of `agreement-dashboard-prd-v2.md`), normalizes responses, computes alerts, KPIs, chart view models, and the financial table, and returns JSON only through **`google.script.run`** handlers that **re-check** spreadsheet authorization. Credentials live in Script Properties **`FIBERY_HOST`** and **`FIBERY_API_TOKEN`**; thresholds and palettes live in `src/agreementThresholds.js` with optional Script Property overrides (FR-56a).
- The solution **MUST NOT** persist Fibery dashboard payloads in **Script Properties**, **Sheet tabs**, or **Drive files** for the purpose of serving this dashboard (other operational logging remains under **FR-40**).
- The **client** caches the last successful JSON in **`sessionStorage`** under key **`fos_agreement_dashboard_v2`** (`cacheSchemaVersion: 2`); payload includes `fetchedAt`, `ttlMinutes`, and the precomputed `kpis`, `alerts`, `charts`, and `financialTable` blocks.
- A **Refresh** control **MUST** trigger a new server fetch; on success it **MUST** replace the cache and update **last refreshed**; on recoverable failure it **SHOULD** keep showing the prior cache when present while surfacing an error state.
- The client honors a **configurable TTL** stored in `localStorage` under **`fos_agreement_dashboard_ttl_minutes_v1`** (default 10 min, options 5 / 10 / 30 / Off, FR-56b). When the panel is opened with a stale cache, the cache is rendered first and a background refresh is dispatched.

### 7.2 Branding and layout (normative)

The Agreement Dashboard panel **MUST** follow **`agreement-dashboard-prd-v2.md` §9.5 Design System**, **§9.6 Brand Identity**, and **§9.7 Layout**: CSS variables (e.g. `--bg` `#061B30`, `--surface` `#092747`, `--accent` `#52C9E5`, `--text` `#FFFEFC`, `--border` `#1a4060`), **Inter** (weights 400–800, base 14px), **deep navy** surfaces (not pure black), **danger `#fc5c65`** reserved for errors and negative margin (and related alert severity), cards **20px** padding / **12px** radius / **1px** border, primary grid **≤900px** single column, **4px** custom scrollbar track using border color. The harpin logo (`https://harpin.ai/wp-content/uploads/logo.svg`) is rendered **once** in the sidebar (`.fos-brand-logo`); as of v1.13.0 the Agreement Dashboard panel header carries **only** the page title `Agreement Management Dashboard` and the subtitle — no in-panel logo and no `.agreement-logo-sep` divider (per **FR-50**).

### 7.3 Components and charts

Implemented widgets MUST match **`agreement-dashboard-prd-v2.md` §7** and colors/thresholds in **§8**. Charting **MAY** use **Chart.js** or other **CDN** libraries suited to HtmlService instead of **Recharts** / bundled **D3**, provided mappings remain consistent with **§8.2–§8.5**. As of v1.10.0 the Agreement Dashboard panel ships the full §7 surface — header (§7.1), KPI strip (§7.2), Status Donut (§7.3), Customer Bar (§7.4), Financial Performance Table (§7.5), Customer Relationship Cards (§7.6), Attention Items Panel (§7.7), Forward Revenue Pipeline (§7.8), Revenue Recognition Progress (§7.9), Type Mix Donut (§7.10), and Revenue Flow Sankey (§7.11) — see FR-56 / FR-56c.

## 8) Utilization Management Dashboard (Operations panel — Fibery Labor Costs)

This section defines how the **Utilization Management Dashboard** (route id `operations`, panel `#panel-operations`) behaves in the Google Apps Script Web App. The full feature spec — data source field paths, computed values §U.1–§U.11, visualizations §N.1–§N.12, drill-down model, and phased delivery plan — lives in **`docs/features/005-utilization-management-dashboard.md`**. This PRD section is the **normative contract** for behaviors that must remain stable across releases; the feature doc is the **delivery plan**.

### 8.1 Data and caching rules

- **Fibery** is the **source of truth** for utilization data. The server reads `Agreement Management/Labor Costs` rows through **`UrlFetchApp`** against the REST **`/api/commands`** endpoint via **batched + paginated** `fibery.entity/query` calls (§"Server contract" in 005-utilization-management-dashboard.md). Credentials live in Script Properties **`FIBERY_HOST`** and **`FIBERY_API_TOKEN`**; thresholds and palettes live in `src/utilizationThresholds.js` with optional Script Property overrides (FR-74 / FR-75).
- The solution **MUST NOT** persist utilization payloads in **Script Properties**, **Sheet tabs**, or **Drive files** for the purpose of serving this dashboard (FR-70).
- The **client** caches the last successful JSON in **`sessionStorage`** under key **`fos_utilization_dashboard_v1`** (`cacheSchemaVersion: 1`); payload includes `fetchedAt`, `ttlMinutes`, `range`, the full `rows` array, and the precomputed `kpis`, `dimensions`, `aggregates`, and `pendingApprovals` blocks (FR-72 / FR-73).
- A **Refresh** control re-invokes the server fetch with the current date range. The configurable **TTL** (5 / 10 / 30 / Off, default 10 min) is persisted in `localStorage` under **`fos_utilization_dashboard_ttl_minutes_v1`**. Opening the panel with a stale cache renders cache first, then dispatches a background refresh (FR-74). The server seed for the TTL minutes is exposed via **`getUtilizationCacheTtlMinutes()`** (Script Property **`UTILIZATION_CACHE_TTL_MINUTES`**, default 10).

### 8.2 Branding and layout (normative)

The Operations panel MUST follow the same brand-token contract as the Agreement Dashboard (FR-50, **`agreement-dashboard-prd-v2.md` §9.5–§9.7**): root CSS variables (`--bg`, `--surface`, `--surface2`, `--border`, `--accent`, `--accent2-4`, `--text`, `--text-muted`, `--danger`, `--warn`), **Inter** at 14px base, page-title row, cards at **20px** padding / **12px** radius / **1px** border, and the 4px custom scrollbar. The panel root carries the **`.fos-agreement-root`** alias class so the existing `--ag-*` token aliases and `.fos-section-card` / `.fos-agreement-kpi` styles apply verbatim — that class is intentionally generic-but-historically-named so the two dashboards stay visually consistent without code duplication. As of v1.13.0 the Operations panel header carries **only** the page title `Utilization Management Dashboard` and the subtitle — no in-panel logo and no `.agreement-logo-sep` divider (per **FR-50**); the sidebar `.fos-brand-logo` is the single brand surface.

### 8.3 Components and charts (Phase A + Phase B + Phase C)

Phase A + Phase B + Phase C render, in this order, inside `#panel-operations`:

1. **Header row** — title "Utilization Management Dashboard", subtitle `"{totalHours} hours · {rowCount} entries · range {start}–{end}"` (no in-panel logo as of v1.13.0; sidebar `.fos-brand-logo` is the single brand surface).
2. **Refresh row** — Refresh button, last-refreshed text, Stale badge, Auto-refresh TTL selector, inline error slot.
3. **Filter bar** — Date range preset (default 90 days), Custom range inputs (date + Apply), Customer multi-select, Project multi-select, **Person multi-select** (Phase B), **Role multi-select** (Phase B), Billable segmented toggle, Internal labor segmented toggle, Clear filters; chip row underneath shows the active non-default state (FR-75, FR-77). Filter state persists across reloads in `localStorage` (FR-79).
4. **Utilization alerts** (Phase C) — server-evaluated alert list (severity-sorted Critical → Warning → Informational) surfaced above the KPI strip; each card click pins the Person filter or opens the row drawer per `target` (FR-80).
5. **KPI strip** — 6 cards: Total Hours, Billable Hours, Utilization %, Total Cost, Effective Bill Rate (with `—` and `0% coverage` honest-fallback when no rows in the filter have `User Role Bill Rate`), Pending Approvals.
6. **Hours by Customer** — Chart.js horizontal bar, top-`UTILIZATION_TOP_N_CUSTOMERS` (default 20). Click toggles Customer filter.
7. **Hours by Project** — Chart.js horizontal bar, top-`UTILIZATION_TOP_N_PROJECTS` (default 20), each bar colored by its customer hue. Click toggles Project filter.
8. **Weekly trend** — Chart.js line, X = ISO Monday-anchored week, two series (Total Hours, Billable Hours).
9. **Billable vs Non-billable** — Chart.js stacked bar per week. Clicking a segment toggles the Billable filter.
10. **Hours by Role** (Phase B) — Chart.js doughnut, slices colored from `dimensions.roles[].color` (deterministic by sorted-hours order from `UTILIZATION_ROLE_PALETTE_`). Click toggles Role filter (FR-77).
11. **Hours by Person** (Phase B) — Chart.js horizontal bar, top-`UTILIZATION_TOP_N_PERSONS` (default 20), each bar colored by the person's primary role. Click toggles Person filter (FR-77).
12. **Utilization heatmap** (Phase C) — SVG `(person × ISO week)` grid; rows capped at `UTILIZATION_HEATMAP_TOP_N_PERSONS` (default 30); §U.10 bucket colors; partial-week cells overlaid with a diagonal-hatch `<pattern>`; a heatmap-local **Role multi-select** scopes the grid without disturbing the rest of the dashboard. As of v1.18.0 a cell click opens a modal that lists the contributing labor entries (FR-85); the prior pin-Person + switch-range drill is removed.
13. **Pending Approvals** (Phase C) — beside the heatmap; lists `isPending` rows from the active filter sorted by `startDateTime` desc, capped at 50 visible with **Show all (N)** toggle; amber / red age badges at 7-day / 14-day cuts; click → row drawer (FR-82).
14. **Detail Table** (Phase B) — sortable + paginated (100 rows / page) table of the filtered row-level labor entries with columns Date · Person · Customer · Project · Role · Hours · Cost · Bill rate · Approval (FR-78). As of v1.14.0, each `<tr>` carries `data-row-id` and clicking the row opens the **row-detail drawer** (FR-82).

The off-canvas **row-detail drawer** (Phase C, FR-82) lives at the body root (outside `<main>`) and is opened from the alerts panel, the heatmap (when an alert pins a row), the Pending Approvals widget, or the Detail Table.

## 9) Delivery Dashboard (Delivery panel — Active Projects + per-project monthly P&L)

This section defines how the **Delivery Dashboard** (route id `delivery`, panel `#panel-delivery`) behaves in the Google Apps Script Web App. The full feature spec — Active Projects table contract, lazy monthly P&L fetch, server query shape, completion + margin-variance bucket maps, §M.1–§M.10 monthly computation rules, and the locked Phase A decisions — lives in **`docs/features/006-delivery-project-pnl.md`**. This PRD section is the **normative contract** for behaviors that MUST remain stable across releases; the feature doc is the **delivery plan**.

### 9.1 Data and caching rules

- **Fibery** is the **source of truth** for delivery data. The server reads agreement context through `getAgreementDashboardData()` (reused, no extra round-trip for the projects list), and for each user-selected project it reads `Agreement Management/Labor Costs`, `Agreement Management/Other Direct Costs`, and `Agreement Management/Revenue Item` through batched `fibery.entity/query` calls (see `src/deliveryDashboard.js` `fetchLaborCostsForAgreement_` / `fetchOtherDirectCostsForAgreement_` / `fetchRecognizedRevenueItemsForAgreement_`). Credentials live in Script Properties `FIBERY_HOST` + `FIBERY_API_TOKEN` (FR-52). Thresholds + palettes live in `src/agreementThresholds.js` with optional Script Property overrides.
- The solution **MUST NOT** persist Delivery payloads in Script Properties, Sheet tabs, or Drive files (parallels FR-52 / FR-70). Server-side caching of either the projects list or the per-project monthly P&L is **explicitly out of scope** for Phase A — the browser owns presentation cache.
- The **client** caches the last successful projects-list JSON in `sessionStorage` under key `fos_delivery_dashboard_v1` (`cacheSchemaVersion: 1`); each per-project monthly P&L payload is cached under a distinct key whose suffix is bumped per cache-schema version (`_v1` in Phase A v1.19.0; **`_v2`** as of Phase B v1.20.0 because each month row now carries `projected: bool` + `revenueItems[]` for the FR-95 drill-down). The per-project payload's `cacheSchemaVersion` MUST match the client-known constant; mismatched payloads MUST be treated as a cache miss and re-fetched. Both caches share a single TTL preference persisted in `localStorage` under `fos_delivery_dashboard_ttl_minutes_v1` (5 / 10 / 30 / Off, default 10 min). Manual **Refresh** MUST invalidate **every** `fos_delivery_pnl_*` entry alongside the projects-list cache so the next selection re-fetches.

### 9.2 Branding and layout (normative)

The Delivery panel MUST follow the same brand-token contract as the Agreement Dashboard + Operations panel (FR-50, `agreement-dashboard-prd-v2.md` §9.5–§9.7). The panel root carries the `.fos-agreement-root` alias class so `--ag-*` token aliases, `.fos-section-card`, and `.fos-agreement-kpi` styles apply verbatim. The panel header carries only the page title `Delivery Dashboard` and the subtitle — no in-panel logo (the sidebar `.fos-brand-logo` is the single brand surface). The full-panel loading overlay reuses `.fos-loading-overlay` per AC-27; the per-card monthly P&L spinner uses a scoped `.fos-delivery-pnl-loading` overlay (smaller, in-flow inside the P&L card) so the KPI strip + header remain visible during a background refresh.

### 9.3 Components (Phase A)

Phase A renders, in this order, inside `#panel-delivery`:

1. **Header row** — title "Delivery Dashboard", subtitle `"{count} active project[s] · last refreshed {timestamp}"`.
2. **Refresh row** — Refresh button (invalidates every per-project P&L cache, then re-fetches the projects list), last-refreshed text, Stale badge, Auto-refresh TTL selector, inline error slot.
3. **Active Projects card** — sortable table with columns Project · Customer · Type · Status · Contract value · Recognized · % Complete (progress bar) · Margin (target-variance dot). Default sort = Contract Value desc; nulls last. Active-project semantics governed by `DELIVERY_ACTIVE_STATES` + `DELIVERY_EXCLUDE_INTERNAL` Script Properties.
4. **Profit & Loss card** — initially in the placeholder state ("Select a project above to see its monthly P&L."). On row click:
   1. The **KPI strip** renders synchronously with five chips (Contract value · Revenue recognized · Total cost · Gross profit · Margin) sourced from the cached project row.
   2. The **Monthly P&L grid** loads lazily via `getDeliveryProjectMonthlyPnL(agreementId)`, with a scoped spinner overlay during the fetch. The grid lists one row per calendar month spanning the project's `Duration` with the columns Month · Revenue · Labor · Expenses · Total cost · Margin $ · Margin %. Out-of-range activity months carry an italic `(OOR)` marker; zero-activity months render muted. A `<tfoot>` Lifetime total row aggregates the visible months.
   3. A **Partial data** badge surfaces above the grid when `payload.partial === true`.
   4. A **§M.9 reconciliation caption** surfaces below the grid when summed monthlies diverge from the agreement's lifetime fields by more than 5% (decision M.5).

### 9.4 Required Script Properties

| Property | Default | Purpose |
| --- | --- | --- |
| `DELIVERY_CACHE_TTL_MINUTES` | 10 | Seed TTL for the projects-list + per-project P&L caches. |
| `DELIVERY_ACTIVE_STATES` | (empty) | CSV whitelist of workflow states to treat as "active"; empty falls back to "all states except Closed-Lost". |
| `DELIVERY_EXCLUDE_INTERNAL` | `true` | When `true`, Agreement Type = Internal rows are excluded from the Active Projects table. |
| `DELIVERY_PNL_INCLUDE_PROJECTED_ODC` | `true` (v1.20.0+; Phase A default was `false`) | When `true`, the per-project monthly P&L includes Other Direct Costs of both `Status = Actual` and `Status = Projected` (decision M.2; Phase B FR-94 flipped this default). Operators may opt out by setting it `false` to restore Phase-A actual-only behavior. |
| `DELIVERY_PNL_MAX_LABOR_ROWS` | 10000 | Hard cap on Labor Cost rows fetched per project; the server returns `partial: true` and surfaces a badge when the cap is reached (decision M.6). `0` = unlimited. |
| `DELIVERY_COMPLETION_UNDER_PCT` | 25 | §D.11 boundary between Under (blue) and Building (teal). |
| `DELIVERY_COMPLETION_BUILDING_PCT` | 75 | §D.11 boundary between Building (teal) and On-track (green). |
| `DELIVERY_COMPLETION_OVER_PCT` | 100 | §D.11 boundary above which the bar paints Over (orange). |
| `DELIVERY_MARGIN_VARIANCE_AMBER_PTS` | 5 | §D.10 width of the amber band immediately below Target Margin (in percentage points). |

### 9.5 Components (Phase B — v1.20.0)

Phase B extends the Phase A P&L card with **chart-view + drill-down + projection + CSV + search** affordances. The Active Projects card and refresh row are unchanged; the additions are scoped to the P&L card and to the table header.

1. **Search input** (FR-98) — added to the Active Projects header, right-aligned in the count row. Substring filter against Project name + Customer name; persisted in `fos_delivery_filters_v1` alongside sort + selection.
2. **P&L toolbar** (FR-96 / FR-97) — a single row above the grid containing a Table / Chart `btn-group` (default `table`) and a `Copy CSV` button. The toolbar is hidden in the placeholder state and shown only when the active project has rendered months.
3. **Chart view** (FR-96) — a Chart.js mixed chart that lazy-loads on first activation via `loadChartJs()`. Stacked bar datasets render Labor + Expenses; an overlaid line dataset renders Revenue. Projected months get muted backgrounds (Labor / Expenses) and a muted point fill (Revenue) so the actual vs. projected split is visually distinct. Clicking a bar / data-point in the chart opens the FR-95 drill-down modal for that month.
4. **Projected month markers** (FR-94) — in the Table view, each month row whose `key > current UTC yyyy-mm` carries a `Projected` pill on the month label and an `is-projected` CSS class (muted text + accent stripe). The server payload also tags `projected: true` per row so the chart and CSV consume the same flag.
5. **Per-month Revenue drill-down** (FR-95) — every Revenue cell becomes a focusable button when the payload's `month.revenueItems[]` is non-empty. Clicking opens `#deliveryRevenueMonthModal` (a separate Bootstrap modal distinct from the Agreement Dashboard's `finAgreementModal`) titled `Revenue items — <Month Label>` with rows of `Milestone · Amount · Date · Status`. The status badge palette is shared with `milestoneStatusColor_`. The footer summarizes `Total $X · recognized $Y`.
6. **Copy CSV** (FR-96) — serializes the visible months + Lifetime total row to a comma-separated payload with columns `Month, Revenue, Labor, Expenses, Total cost, Gross profit, Margin %, Projected, Out of range` and writes it to the clipboard via `navigator.clipboard.writeText` (with a textarea + `execCommand('copy')` fallback). Status flashes in the toolbar for ~3 s.
7. **Activity events** — Phase B adds four new event types, all on Route `delivery`: `delivery_pnl_view_toggle` (`view=<table|chart>`), `delivery_pnl_month_drilldown` (`monthKey=<key> · itemCount=<n>`), `delivery_pnl_copy_csv` (`agreementId=<id> · rows=<n>`), and `delivery_table_search` (`len=<trimmedLength>`; fires only on non-empty terms, debounced 400 ms).
8. **Server contract changes** — `getDeliveryProjectMonthlyPnL(agreementId)` (a) drops the `Revenue Recognized = true` filter and returns ALL revenue items (recognized + unrecognized) with `name`, `recognized`, and `state`; (b) defaults `DELIVERY_PNL_INCLUDE_PROJECTED_ODC` to `true`; (c) adds `projected: bool` and `revenueItems: [{ id, name, amount, recognized, targetDate, actualDate, state }]` to each `months[i]`; (d) bumps `DELIVERY_PNL_CACHE_SCHEMA_VERSION_` to **2** (client cache key suffix moves from `_v1` to `_v2`).

### 9.6 Components (Phase C — v1.21.0)

Phase C is **client-only** for the Delivery panel (no new `google.script.run` endpoints) plus **Agreement Attention** rule extensions server-side.

1. **Pacing strip** (FR-99) — `#delivery-pnl-pacing` toggles visible when monthly P&L rows exist, the KPI strip is shown, and a duration window yields a timeline pct; hidden on deselect / clear / missing duration math.
2. **Delivery signals** (FR-99) — `#delivery-attention-section` + `#delivery-attention-list`; hidden when the heuristics produce zero cards for the current visible row set.
3. **Portfolio margin-flow Sankey** (FR-99) — `#delivery-sankey-wrap` hosts the diagram; empty state when visible non-Internal recognized revenue sums to zero.
4. **Agreement Attention rules** (FR-100) — `evaluateAlerts_()` gains pacing / cost / near-end rules; Fibery query count for the Agreement Dashboard is unchanged.

## 10) Relationship to Prior Google Workspace Work

The **Clockify to Fibery Sync** product (see `docs/PRD.md`) remains the **system of record pipeline** for time data into Fibery and related staging sheets. The **FOS Dashboard**:

- **Reads** from curated layers (Sheets tabs, Fibery queries, APIs) rather than re-implementing sync logic.
- **Does not** replace incremental checkpoints, labor staging tabs, or Fibery push semantics defined in that PRD.
- **May** surface operational health derived from those processes (e.g. last sync timestamp from a shared log tab) when agreed and implemented.

## 11) Acceptance Criteria

- AC-01 **[Backlog]**: A user in the allowed domain opens the Web App and sees the dashboard shell with at least one configured KPI section.
- AC-02 **[Backlog]**: **Refresh** (or initial load) populates KPI values from the configured metric layer without client-side secrets.
- AC-03 **[Backlog]**: When a connector fails, the affected panel shows a clear error or stale state and other panels still render if their data succeeded.
- AC-04 **[Backlog]**: Finance-relevant numbers use correct units (currency, percent, count) per metric definition.
- AC-05 **[Backlog]**: A successful refresh cycle writes a **log row** (or equivalent) with timestamp and per-source outcome for admin review.
- AC-06 **[Backlog]**: The UI displays **last updated** metadata that matches the server’s latest successful refresh for that panel or global scope.
- AC-07 **[Backlog]**: README (or project index doc) links to this PRD as the dashboard requirements baseline.
- AC-08 **[Released]**: The published UI shows the **PRD version** string (sidebar + not-authorized page; version constant in code).
- AC-09 **[Released]**: A user whose email appears in the configured **users** tab receives the **dashboard shell**; the server response includes **role** and **team** derived from that row (shown in the user chip from `getDashboardNavigation`).
- AC-10 **[Released]**: A user who is **signed in** but **not** listed in the users tab receives only the **not authorized** page with a clear message that access has not been granted; they do **not** see dashboard navigation or KPI placeholders.
- AC-11 **[Released]**: With required Script Properties **missing** for the authorization sheet, the deployment fails **closed** (user sees the configuration-oriented message on the not-authorized page **without** exposing internal keys).
- AC-12 **[Released]**: `getDashboardNavigation` **re-checks** authorization via `requireAuthForApi_()` and throws if the user is not on the sheet, so the client does not receive the navigation model when unauthorized.
- AC-13 **[Released]**: The left nav shows **icons + labels** for each dashboard entry, and the sidebar footer shows **Settings** with a **gear** icon; inactive targets still open the **coming soon** modal.
- AC-14 **[Released]**: The **Agreement Dashboard** nav entry (route id `agreement-dashboard`, panel `#panel-agreement-dashboard`) opens the agreement dashboard panel (not the generic “coming soon” modal) with **Refresh**, **last refreshed** text, **sessionStorage** cache of the last successful server payload (key `fos_agreement_dashboard_v2`, `cacheSchemaVersion: 2`), and visuals aligned with **`## 7)`** subsection **7.2**; `getAgreementDashboardData` re-checks authorization, runs the §4 Fibery queries in one batched `/api/commands` round-trip, and returns live KPIs, alerts, chart view models, and the financial table. When `FIBERY_HOST` or `FIBERY_API_TOKEN` are unset (or Fibery is unreachable), the call returns `ok:false` with a friendly message and the UI keeps the prior cache rendered.
- AC-15 **[Released]**: When an authorized user opens the Web App, the server appends a `page_load` row to the **`User Activity`** tab containing **Timestamp**, **Email**, **Role**, **Team**, and the event metadata defined in **FR-62**; an unauthorized session writes **no** row (the not-authorized page does not log).
- AC-16 **[Released]**: When an authorized user navigates between dashboard panels (Home, Agreement Dashboard, Operations, Delivery) or invokes the agreement-dashboard **Refresh**, the client calls the server logging handler and a corresponding `nav_view` or `refresh` row appears in the **`User Activity`** tab tagged with the **Route** (e.g. `agreement-dashboard` for the Agreement Dashboard entry; rows with `Route = finance` are pre-v1.11.0 historical data) and the user’s **Session ID**. The agreement-dashboard financial-table tab clicks log under the `agreement_table_tab` event type (renamed from `finance_table_tab` in v1.11.0).
- AC-17 **[Released]**: With **`USER_ACTIVITY_LOGGING_ENABLED`** set to `false`, no new rows are written to the **`User Activity`** tab for any event, and dashboard navigation continues to work without user-visible errors; with the property unset or `true`, logging is on by default.
- AC-18 **[Released]**: The dashboard shell, not-authorized page, Home panel, “coming soon” modal, and Agreement Dashboard panel all draw colors and typography from the same root CSS variables defined per **`agreement-dashboard-prd-v2.md` §9.5** (declared once at `:root` in `src/DashboardShell.html` and again in `src/NotAuthorized.html`); the `--ag-*` family inside `.fos-agreement-root` resolves via aliases to those globals, and **no** `--harpin-navy*` / `--harpin-teal*` legacy tokens remain. The **Settings** control at the bottom of the sidebar is an `<a>` element using the `.fos-nav-btn` class (no outlined or pill chrome), with its click handler calling `preventDefault()` before opening the **coming soon** modal.
- AC-19 **[Released]**: With Fibery credentials configured, the Agreement Dashboard panel renders, in addition to the KPI strip, the **Attention items** panel (sorted Critical → Warning → Informational, all-clear card when empty), the **Agreement status** donut, the **Agreement type mix** donut, the **Revenue recognition progress** stacked bar (top N agreements, default 10, excluding Internal), the **Customer contract value** horizontal bar (non-Internal companies with ≥ 1 agreement), and the **Financial performance** table with three working tabs (**All active**, **Top customer**, **Other customers**) including margin bars colored per §8.4. The **Auto-refresh** selector defaults to **10 min**, persists across reloads via `localStorage`, surfaces the **Stale** badge when the cache exceeds the TTL, and triggers a background refresh when the panel is opened with a stale cache; setting the selector to **Off** suppresses both the stale indicator and any auto-refresh.
- AC-20 **[Released]**: The Agreement Dashboard panel renders the **Customer relationships** section (§7.6) as a scrollable vertical list with one card per company — initials avatar in the company's §8.5 palette color, full name, agreement count, funnel stage, segment(s), an NDA badge (green check when `NDA Completed = true`), and the formatted Total Customer Contract Value (or the literal text "Internal" for internal entities) — sorted by TCV desc with internal companies pushed to the bottom. The Agreement Dashboard panel additionally renders the **Forward revenue pipeline** section (§7.8) as one row per non-Internal agreement with future revenue items (or active agreements with no future items), each row showing the §5.5 monthly billing rate as a horizontal bar in the customer's palette color, scaled to the dataset maximum; rows whose scheduling status is `Not Scheduled` / `No Pipeline Items` / `Partially Scheduled` render at reduced opacity with a dashed border and an inline note, sorted by monthly rate desc.
- AC-21 **[Released]**: With Fibery credentials configured, the Agreement Dashboard panel renders the **Revenue flow Sankey** (§7.11) below the financial table: three node columns (Status → Customer → Type), node fills from §8.2 / §8.5 / §8.3, link bands as `source → target` gradients at `AGREEMENT_SANKEY_LINK_OPACITY` (default 0.35), node and link tooltips showing portfolio percentages, diagram height `max(400, node_count × 28)` capped at 900px, internal-type agreements excluded unless `AGREEMENT_SANKEY_INCLUDE_INTERNAL = true`. D3 v7 and d3-sankey are lazy-loaded from jsDelivr only when the Agreement Dashboard panel renders; on load failure the section shows a non-fatal message and the rest of the panel remains usable.
- AC-22 **[Released]**: The **Operations** nav entry (route id `operations`, panel `#panel-operations`) opens the **Utilization Management Dashboard** (no longer the generic "coming soon" modal) with the header / refresh row / filter bar / six-card KPI strip / Hours-by-Customer bar / Hours-by-Project bar / Weekly trend line / Billable-vs-Non-billable stacked bar visible. `getUtilizationDashboardData` re-checks authorization, fetches paginated `Agreement Management/Labor Costs` for the default 90-day window when no range is passed, and the cache (key `fos_utilization_dashboard_v1`, `cacheSchemaVersion: 1`) is written on success. When `FIBERY_HOST` / `FIBERY_API_TOKEN` are unset (or Fibery is unreachable), the call returns `ok:false` with a friendly message and the UI keeps the prior cache rendered. The **Auto-refresh** selector defaults to **10 min**, persists across reloads via `localStorage` (`fos_utilization_dashboard_ttl_minutes_v1`), surfaces the **Stale** badge when the cache exceeds the TTL, and triggers a background refresh when the panel is opened with a stale cache; setting the selector to **Off** suppresses both the stale indicator and any auto-refresh.
- AC-23 **[Released]**: In the Operations panel, selecting a **Customer** or **Project** from the multi-select dropdowns — or clicking a bar in **Hours by Customer** / **Hours by Project** — adds that value to the corresponding filter set and every chart + KPI re-aggregates client-side from the cached `rows` (no server round-trip). Toggling **Internal labor → Exclude** removes rows where `isInternal = true` from every chart + KPI and surfaces a chip reading `Excluding internal labor` in the active-filter row. The **Billable** and **Internal labor** toggles compose independently. When **all** rows in the filtered view lack `User Role Bill Rate`, the **Effective Bill Rate** KPI displays `—` with `0% coverage` rather than `$0/hr` or `NaN`. Every filter / drill change is activity-logged under event types `util_filter` / `util_drill` with `Route = operations`.
- AC-24 **[Released]**: In the Operations panel, the **Hours by Role** doughnut and the **Hours by Person** horizontal bar are rendered alongside the Phase A charts. Selecting a Role or Person from the corresponding multi-select dropdown — or clicking a slice / bar — adds that value to the filter set and every chart + KPI + Detail Table re-aggregates client-side. Active Person and Role filters render as removable chips (`Person: …` / `Role: …`) with `×`. **Clear filters** resets Customer + Project + Person + Role + Billable + Internal labor (range is preserved). Each click is activity-logged under `util_drill` with `Route = operations`.
- AC-25 **[Released]**: The Operations panel renders a **Detail Table** below the charts with the columns Date · Person · Customer · Project · Role · Hours · Cost · Bill rate · Approval. The table reflects the active filter at all times. Clicking any column header toggles between ascending and descending sort; null / empty values sort to the bottom regardless of direction. The pager exposes Prev / Next buttons + a "{start}–{end} of {total} · page {n} of {N}" label; page size is **100 rows**. The Approval cell renders as a colored pill (Approved teal, Pending amber, Unapproved red, Unknown grey). Sort and page-navigation events are activity-logged under event type `util_detail` with `Route = operations`.
- AC-26 **[Released]**: The Operations panel persists filter state across page reloads via `localStorage` key **`fos_utilization_filters_v1`** (`schemaVersion: 1`). The persisted shape includes `customers` / `projects` / `persons` / `roles` (array of selected keys) and `billable` / `internalLabor` (segmented values). The **`range`** is intentionally NOT persisted — opening the panel always uses the default 90-day window unless the user changes the date preset. Schema-version mismatches cause the stored value to be ignored; write failures (private mode / quota) are swallowed silently. Neither the Agreement Dashboard panel nor the Operations panel renders an in-panel harpin logo or `.agreement-logo-sep` divider as of v1.13.0 — the sidebar `.fos-brand-logo` is the single rendered brand mark on the page; the panel headings (`Agreement Management Dashboard` / `Utilization Management Dashboard`) and subtitles remain in their respective `.fos-agreement-inner` containers.
- AC-28 **[Released]**: **Utilization alerts panel (v1.14.0; patched v1.14.1).** The Operations panel renders a `Utilization alerts` section above the KPI strip listing one card per server-evaluated `alerts[]` entry. The list reflects severity sort order (Critical red → Warning amber → Informational teal), surfaces the rule `title` + `body` from `src/utilizationAlerts.js`, and always renders at least the "No utilization alerts" informational card. Each card is clickable: clicking an `under_utilized` / `over_allocated` card pins the global Person filter to `target.person` (cross-filters the rest of the dashboard); clicking a `stale_approval` card opens the row-detail drawer for `target.rowId`; clicking a `stale_approval_rollup` card flips the Pending Approvals widget into Show-all mode and scrolls to it. As of v1.14.1, the Alerts panel MUST cap individual `stale_approval` cards at the 20 oldest and emit a single `stale_approval_rollup` card for any remainder. As of v1.14.1, an Approved row whose `Time Entry Status` is blank MUST NOT be flagged as pending (was the v1.14.0 false-positive bug producing thousands of bogus stale-approval cards). Every click emits a `util_alert_click` activity-log event (`Route = operations`) carrying severity + kind.
- AC-29 **[Released]**: **Utilization heatmap (v1.14.0).** The Operations panel renders an SVG heatmap of `(person × ISO week)` cells using the §U.10 bucket colors (`< 60%` red, `60-85%` amber, `85-110%` green, `> 110%` orange, no-data `#0d2e4a`). Rows are sorted by total hours desc and **capped at `UTILIZATION_HEATMAP_TOP_N_PERSONS` (default 30)**. A header meta line surfaces `{N} people · {M} weeks` and `· {K} more hidden (cap N)` when the cap clips. Weeks that overlap the active-range edge render the bucket color overlaid with a diagonal-hatch SVG `<pattern>`, and the `<title>` tooltip notes the in-range fraction. The heatmap section includes its own **Role multi-select dropdown** (independent of the global Role filter) whose contents come from `dimensions.roles` sorted by hours desc — changes to this dropdown re-render the heatmap only. The global Internal-labor toggle (Include / Exclude) MUST apply to the heatmap when active. Clicking a cell pins the global Person filter to that person and switches the active range to that week's `weekStartIso` / `weekEndIso`, triggering a fresh server fetch, AND emits a `util_heatmap_drill` activity-log event. The legend below the heading lists all bucket thresholds plus the partial-week swatch.
- AC-30 **[Released]**: **Pending Approvals widget (v1.14.0).** The Operations panel renders a `Pending approvals` widget beside the heatmap listing the rows where `isPending = true` from the active filtered set, sorted by `startDateTime` desc. The list is capped at 50 visible rows; a **Show all (N)** button toggles between the capped view and the full list (button labels swap between "Show all (N)" and "Show top 50"). Each row renders an age badge (`{N}d`) in amber when `ageDays ≥ 7 && < 14` and in red when `ageDays ≥ 14`, the person + date title, the customer + project meta, and the hours total. Clicking any row opens the row-detail drawer and emits a `util_drawer_open` activity-log event (`source=pending`). The widget exposes an empty-state message ("No pending rows match the current filter.") when the filtered set yields zero pending rows.
- AC-31 **[Released]**: **Row-detail drawer (v1.14.0; URL composition revised v1.15.0).** The Operations panel mounts an off-canvas `.fos-util-drawer` panel anchored to the right edge (z-index `1100`, slide-in transition `0.22s ease`, suppressed under `prefers-reduced-motion: reduce`) with a translucent backdrop (`.fos-util-drawer-backdrop`, z-index `1090`). The drawer renders the normalized row in a definition list with the labels Date · Person · Role · Customer · Project · Task · Hours · Cost · Bill rate · Cost rate · Margin/hr · Billable · Approval · Time-entry status · Created · Approved-at · Agreement · Agreement state · Agreement type · Fibery id. The footer MAY render an **Open in Fibery →** anchor when **all** of the following hold (see AC-36 / AC-37): the signed-in user has `fibery_access = TRUE` in the Users sheet; the server surfaced a non-null `nav.fibery` config block; the row carries both `publicId` and `name`. Otherwise the anchor + its footer MUST be hidden (`.fos-util-drawer-footer` flagged `d-none`). The drawer closes on the close button, on backdrop click, AND on Escape, and the close handler hides both the drawer and the backdrop.
- AC-32 **[Released]**: **Cache schema v1 → v2 (v1.14.0).** The client-side cache schema version constant is bumped from `1` to `2`; clients holding a v1 payload from a prior session MUST silently drop the cache and refetch on the next `showOperations()` call. The new payload shape adds `aggregates.byPersonWeek` (one entry per `(personKey, week)` carrying `hours`, `billableHours`, `capacityHours`, `utilizationPct`, `partial`, `partialFraction`, `roles[]`, `customers[]`, `weekStartIso`, `weekEndIso`, `isInternal`) and `alerts[]` (one entry per surfaced rule hit, see FR-80). The Phase B fields (KPIs, dimensions, the original aggregates, `pendingApprovals`) are preserved unchanged so the existing Phase A + B renderers continue to read from the same shape.
- AC-33 **[Released]**: **Phase C activity logging (v1.14.0; heatmap event renamed v1.18.0).** Four new event types MUST be emitted from the Operations panel and threaded through `logActivity_`: `util_alert_click` (severity + kind), `util_heatmap_drill` (person + week) — **as of v1.18.0 this event is retired and replaced by `util_heatmap_cell_click` per AC-40**, `util_heatmap_role` (add / remove role from the heatmap-local filter), `util_pending_toggle` (`show_all` vs `show_top`), `util_drawer_open` (source = alert / pending / detail_table + rowId). The Route MUST be `operations` on all five. Logging failures (network / quota) MUST be swallowed silently and MUST NEVER block UI.
- AC-34 **[Released]**: **Configurable thresholds (v1.14.0).** Three new Script Properties MUST be readable by `getUtilizationThresholds_()`: `UTILIZATION_HEATMAP_TOP_N_PERSONS` (default 30), `UTILIZATION_STALE_APPROVAL_WARN_DAYS` (default 7), and `UTILIZATION_STALE_APPROVAL_CRIT_DAYS` (default 14). The resolver MUST enforce `staleApprovalCritDays > staleApprovalWarnDays` (rounding the critical value up when an admin misconfigures the pair); all three MUST coerce to positive integers via `parsePositiveNumber_`. The heatmap row cap MUST NOT silently override the existing `UTILIZATION_TOP_N_PERSONS` for the §N.7 person bar — they are independent.
- AC-35 **[Released]**: **Editor diagnostics (v1.14.0).** A new `_diag_sampleUtilizationAlerts()` editor helper MUST exist in `src/fiberyUtilizationDashboard.js` that fetches the default range, runs the full normalization → aggregate → alerts pipeline, and logs `{rowCount, byPersonWeekCount, alertCount, countsByKind, top5}` to the Apps Script execution log. It MUST not require any client-side UI to surface its output.
- AC-36 **[Released]**: **Per-user Fibery access gate (v1.15.0).** Given the Users sheet has a `fibery_access` header (or the override `AUTH_COL_FIBERY_ACCESS`), when the signed-in user's matching row carries a truthy value (`TRUE`, `True`, `true`, `yes`, `y`, `1`, or boolean `true`), the navigation payload returned by `getDashboardNavigation()` MUST set `fiberyAccess: true` AND attach `nav.fibery = { scheme, host, laborCostPathTemplate }`. For every other case — header missing, cell blank, cell value `FALSE` / `0` / `no` / garbage, or the row doesn't exist — the payload MUST set `fiberyAccess: false` AND MUST NOT include `nav.fibery`. The client's row-detail drawer MUST hide the **Open in Fibery →** anchor (collapsing `.fos-util-drawer-footer` via `d-none`) for every gated user, and MUST surface the anchor only when the row's `publicId` and `name` are both present.
- AC-37 **[Released]**: **Server-composed deep-link URL (v1.15.0).** The Operations row-detail drawer URL MUST be composed via `scheme + '://' + host + template.replace('{slug}', name.trim().replace(/\s/g, '-')).replace('{publicId}', publicId)`. With default Script Properties (`FIBERY_PUBLIC_SCHEME=https`, `FIBERY_LABOR_COST_PATH_TEMPLATE=/Agreement_Management/Labor_Costs/{slug}-{publicId}`) and `FIBERY_HOST=harpin-ai.fibery.io`, a row with `name = "2026-03-20 - Alex Anakin - 0.5 hrs"` and `publicId = "167141"` MUST resolve to `https://harpin-ai.fibery.io/Agreement_Management/Labor_Costs/2026-03-20---Alex-Anakin---0.5-hrs-167141`. The anchor MUST open in a new tab (`target="_blank" rel="noopener"`) and MUST emit a `util_drawer_fibery_click` activity-log event (route = `operations`, label = `rowId=<id>`). Clicking it MUST NOT block navigation if the logging call fails. The Script Property `FIBERY_DEEP_LINK_HOST` MAY override the host without affecting API calls (`FIBERY_HOST` remains the API host); when both are unset the server MUST return `null` from `getFiberyDeepLinkConfig_()` and the nav payload MUST omit `nav.fibery` even for users with `fiberyAccess: true`, which forces the client to hide the anchor (graceful degradation).
- AC-38 **[Released]**: **Grouped Utilization Alerts (v1.16.0; grouping axis revised v1.17.0).** The Operations panel's Utilization Alerts panel MUST render alerts as collapsible groups (`.fos-attention-group`) instead of flat cards. **As of v1.17.0** the grouping key MUST be the alert **`kind`** (`under_utilized` → "Under-utilized", `over_allocated` → "Over-allocated", `stale_approval` → "Stale approvals"); previously v1.16.0 grouped by `target.person`. Alerts whose `kind ∈ {stale_approval_rollup, all_clear}` MUST be rendered as standalone flat cards **above** the grouped section. The group wrapper MUST carry a `sev-<critical|warning|info>` modifier class matching the worst severity in the group, which MUST drive the border-left color, the header's emoji icon, and the visual ranking; the wrapper MUST also carry `data-kind="<kind>"` so the Collapse all handler (AC-39) can sync state back to `utilState.alerts.expanded`. Groups MUST sort by (1) worst severity, (2) alert count desc, (3) group-label asc. **Default expand state (v1.21.1):** every kind-based group starts **collapsed** on first paint and after each `applyUtilPayload()` reset; a group is **open** only when `utilState.alerts.expanded[kind] === true` (user expanded the header). Manual user toggles (header click OR `Enter`/`Space` when the header is focused) MUST persist in `utilState.alerts.expanded` (keyed by alert `kind` as of v1.17.0) for the duration of the current payload until the next `applyUtilPayload()` clears the map. Each toggle MUST emit a `util_alert_group_toggle` activity-log event (route = `operations`, label format `kind=<kind> · expand|collapse` — revised in v1.17.0 from `personKey=<key>`). The header MUST carry `role="button"`, `tabindex="0"`, `aria-expanded`, and `aria-controls` referencing a stable id on the children container. Clicking an individual alert inside an expanded group MUST preserve the existing `handleUtilAlertClick_` behavior (drawer open / person-filter pin / Pending Approvals scroll) — the inner click MUST stop propagating so it does not toggle the group. Unknown `kind` values MUST be humanized for display (e.g. `priority_review` → "Priority review") so a forward-compatible server can ship a new rule without a client release. The total alert count text (`<N> alerts`) in the panel header MUST continue to reflect the **total** count across both standalone and grouped sections.
- AC-39 **[Released]**: **Collapse all button (v1.17.0).** The Utilization Alerts panel header MUST surface a right-aligned **Collapse all** button (`#util-attention-collapse-all`, reusing the existing `.fos-util-clear-btn` pill chrome to match Pending Approvals' "Show all" affordance) that closes every kind-based group in a single click. Clicking the button MUST (a) remove `.is-open` from every `.fos-attention-group`, (b) set `aria-expanded="false"` on each header, (c) write `false` into `utilState.alerts.expanded[<kind>]` for every group so the override survives subsequent re-renders within the same payload, and (d) emit a `util_alert_collapse_all` activity-log event (route = `operations`, label = `groups=<count-collapsed>`). The button MUST be hidden when no kind-based groups exist (panel-flat with only standalone cards) AND when every group is already collapsed; the visibility computation MUST run on every `renderUtilizationAlerts` invocation AND after every individual group toggle
- AC-40 **[Released]**: **Heatmap-cell modal replaces the drill (v1.18.0; FR-85).** Clicking any non-empty cell in the Utilization Heatmap MUST open `#utilHeatmapCellModal` and MUST NOT mutate `utilState.filters` (no Person pin, no range change, no server fetch). The modal title MUST be the person's name; the subtitle MUST be `Week of <weekIso> · <pct>% utilization` (plus ` · partial week` when `entry.partial === true`); the body MUST list rows filtered from `applyFilters(utilState.payload.rows)` further narrowed to `r.week === entry.week AND personKeyForRow_(r) === entry.personKey`, sorted by `startDateTime` ascending; the footer MUST surface `<count> entries · <sumHours> h · <sumCost>`. A `util_heatmap_cell_click` activity-log event MUST fire on open (Route = `operations`, label = `person=<personName> · week=<weekIso> · rows=<count>`). The pre-v1.18.0 event name `util_heatmap_drill` MUST no longer fire from the Operations panel.
- AC-41 **[Released]**: **Agreement milestones modal (v1.18.0; FR-86).** Every row in the Agreement Dashboard's Financial performance table (across all three tabs: All active / Top customer / Other customers) MUST be a focusable button (`tabindex="0"`, `role="button"`, `aria-label="View milestones for <agreement name>"`). Click and Enter / Space MUST open `#finAgreementModal`. The body MUST be populated from `payload.revenueItemsByAgreement[agreementId]` (server-supplied) — never from a fresh Fibery fetch. Each milestone row MUST render Milestone (name, ellipsis-truncated with a `title` tooltip), Target amount (compact money; falls back to actualAmount when targetAmount is `0`), Target date (ISO `yyyy-mm-dd` slice; `—` when null), and Invoice status (a colored pill keyed off the Revenue Item's `state`). Agreements with zero milestones MUST render the friendly empty message "No milestones found for this agreement." and the totals line MUST collapse to `—`. An `agreement_milestones_open` activity-log event MUST fire on every open (Route = `agreement-dashboard`, label = `agreementId=<id> · items=<count>`).
- AC-42 **[Released]**: **Persons dropdown alpha sort + Fibery link tolerance (v1.18.0; FR-87 + FR-88).** (a) The Operations panel's **Persons** multi-select trigger menu MUST list options in case-insensitive locale-aware alphabetical order on every `renderUtilDashboard()` invocation; the menu order MUST be derived from a `.slice()` clone so the cached `dimensions.persons` array remains in server-supplied hours-desc order for the heatmap. (b) `findHeaderIndex_('fibery_access')` against a sheet whose header is `Fibery Access` or `fibery-access` MUST resolve to the correct column (loose-match fallback); against a sheet missing the column entirely the existing one-time `console.warn` MUST still fire. (c) `getFiberyDeepLinkConfig_()` MUST scrub scheme + trailing slash from the resolved host so `FIBERY_HOST = "https://harpin-ai.fibery.io/"` produces `cfg.host = "harpin-ai.fibery.io"` and a clean single-scheme URL. (d) When `getFiberyLaborCostUrl_(row)` returns `''` the client MUST log a single `[FOS] Open-in-Fibery hidden: <reason>` warning to the browser console with the specific failure branch named; the production UI MUST still hide the anchor silently. (e) `_diag_fiberyAccess()` invoked from the Apps Script editor MUST return a JSON object with `email`, `authOk`, `fiberyAccess`, `deepLinkConfig`, `sampleUrl`, and a `notes[]` array of human-readable hints (empty when everything looks healthy). so the affordance disappears once it has nothing to act on. Standalone cards (rollup / all-clear) MUST be unaffected by the button — they have no expand state. The Collapse all button MUST NOT clear `utilState.alerts.expanded` entirely (so default-open groups re-expand on the next payload) — it only writes `false` per kind for the currently-rendered groups.
- AC-43 **[Released]**: **Delivery panel activation + Active Projects table (v1.19.0; FR-89).** The Delivery nav entry (route id `delivery`, DOM panel `#panel-delivery`) MUST open the Delivery Dashboard with header + refresh row + Active Projects table + placeholder P&L card visible. `getDeliveryDashboardData()` MUST reuse `getAgreementDashboardData()` server-side (zero additional Fibery queries for the projects list); the cache (key `fos_delivery_dashboard_v1`, `cacheSchemaVersion: 1`) MUST be written on success. The default table sort MUST be Contract Value desc; clicking a sortable column header MUST toggle that column between asc and desc (numeric columns default to desc on first activation; text columns default to asc); nulls MUST sort last regardless of direction. Sort state MUST persist across reloads in `localStorage` `fos_delivery_filters_v1` (`schemaVersion: 1`). The `% Complete` cell MUST render a §D.11 colored progress bar (Under / Building / On-track / Over) and the Margin cell MUST render a §D.10 variance-bucket dot beside the `formatMarginPct` value. The Auto-refresh selector MUST default to 10 min, persist in `localStorage` `fos_delivery_dashboard_ttl_minutes_v1`, surface the Stale badge when the cache exceeds the TTL, and trigger a background refresh on panel-open when stale; setting the selector to Off MUST suppress both the stale indicator and any auto-refresh.
- AC-44 **[Released]**: **Row selection + KPI strip (v1.19.0; FR-90).** Clicking any row in the Active Projects table (and Enter / Space when the row has focus) MUST mark the row `is-selected` (visible accent strip), persist the selected `agreementId` in `fos_delivery_filters_v1`, and reveal the **Profit & Loss** card below with the KPI strip rendered synchronously from the cached project row. The five KPI chips MUST be Contract value · Revenue recognized · Total cost · Gross profit · Margin (with sub-caption tgt `<pct>%` when a Target Margin exists, else "No target"). Total cost MUST be rendered parenthesized as a positive number; Gross profit MUST be rendered parenthesized when negative AND the chip MUST add `is-negative`. A second click on the same row MUST deselect it (`is-selected` removed, KPI strip cleared + hidden, subtitle reverts to "Select a project above to see its monthly P&L."). Selection MUST emit a `delivery_project_select` activity-log event (Route = `delivery`, label = `agreementId=<id> · name=<truncated>`); deselection MUST emit `delivery_project_deselect`.
- AC-45 **[Released]**: **Monthly P&L lazy fetch + cache (v1.19.0; FR-91).** Selecting a project MUST trigger `getDeliveryProjectMonthlyPnL(<agreementId>)` lazily and cache the response in `sessionStorage` under `fos_delivery_pnl_<agreementId>_v1` (`cacheSchemaVersion: 1`). When a fresh cache exists, the grid MUST render synchronously from cache without showing the spinner; when the cache is stale (per the shared TTL) AND a cached copy exists, the grid MUST render from cache first AND a background refresh MUST fire. When no cache exists, the scoped `.fos-delivery-pnl-loading` overlay MUST cover the grid area (but not the KPI strip or header) until the fetch completes. The server endpoint MUST issue exactly three per-agreement Fibery queries (Labor Costs, Other Direct Costs filtered to Status = Actual unless `DELIVERY_PNL_INCLUDE_PROJECTED_ODC = true`, Revenue Items filtered to Revenue Recognized = true) and aggregate them into one row per calendar month. Out-of-range activity MUST render with an italic `(OOR)` marker on the month label; zero-activity months within the project Duration MUST render as muted zero rows. A stale-response guard MUST prevent rendering when the user has selected a different project while a fetch was in flight.
- AC-46 **[Released]**: **Monthly grid display contract (v1.19.0; FR-91).** The Monthly P&L grid MUST display columns Month · Revenue · Labor · Expenses · Total cost · Margin $ · Margin %. Labor, Expenses, and Total cost MUST be rendered parenthesized as positive numbers (ledger convention); Margin $ MUST be rendered parenthesized when negative AND its cell MUST add `is-negative`. The Margin % cell MUST receive the §D.10 bucket-coloring class (`bucket-green` / `bucket-amber` / `bucket-red` / `bucket-neutral`) keyed off `marginVarianceBucket_(month.marginPct, agreement.targetMargin, ...)`. A `<tfoot>` Lifetime total row MUST aggregate the visible months (Revenue / Labor / Expenses / Total cost / Gross profit / Margin %), with Margin % derived from the summed monthlies. An empty months array MUST hide the grid and surface the empty-state message "No monthly activity for this project yet."
- AC-47 **[Released]**: **Operational signals: partial-data badge + reconciliation caption (v1.19.0; FR-92).** When `payload.partial === true` (Labor Cost row cap reached), the P&L card MUST surface a `Partial data — capped at <N> rows` badge above the grid with a tooltip explaining the cap. When summed monthlies diverge from the agreement's lifetime fields by more than **5%** (decision M.5), a one-line reconciliation caption MUST surface below the grid prefixed with `ⓘ Reconciliation —` and listing each diverging metric (Labor, Expenses, Margin) with its summed value, the agreement's lifetime value, and either the percent delta (Labor / Expenses) or the points delta (Margin %). Below the 5% threshold the caption MUST remain hidden. Server errors during the monthly P&L fetch MUST surface in `#delivery-pnl-error` (red, `role="alert"`) without clearing the synchronously-rendered KPI strip.
- AC-48 **[Released]**: **Delivery activity logging + Refresh cache invalidation (v1.19.0; FR-93).** Eight new activity-log event types MUST be emitted from the Delivery panel and threaded through `logActivity_`, all with Route = `delivery`: `delivery_panel_open`, `delivery_refresh` (label `manual=true`), `delivery_project_select`, `delivery_project_deselect`, `delivery_table_sort`, `delivery_pnl_fetch_start`, `delivery_pnl_fetch_done` (label includes `monthCount` + `ms` elapsed), `delivery_pnl_fetch_error` (label includes the failure code). The manual Refresh button MUST invalidate every `sessionStorage` key matching `fos_delivery_pnl_*_v1` AND clear `deliveryRenderState.lastRenderedFetchedAt` before issuing the projects-list re-fetch — so the next project selection triggers a fresh P&L fetch. Logging failures (network / quota) MUST be swallowed silently and MUST NEVER block UI. The 250 ms event-throttle window (FR-65) applies — quick double-clicks MUST NOT emit duplicate select events.
- AC-49 **[Released]**: **Projected months + cache v2 (v1.20.0; FR-94).** The server payload MUST drop the recognized-only filter on `Revenue Item`, default `DELIVERY_PNL_INCLUDE_PROJECTED_ODC` to `true`, emit `month.projected = true` whenever the month key is greater than the current UTC `yyyy-mm`, and bump `cacheSchemaVersion` to **2** (client cache key suffix `_v2`). Client cache reads with `cacheSchemaVersion: 1` MUST be treated as a miss and re-fetched. The Monthly P&L grid MUST display a `Projected` pill on each projected month label and tag the row with `is-projected` (muted text + accent stripe). The Copy CSV payload (AC-51) MUST include `Projected` and `Out of range` columns.
- AC-50 **[Released]**: **Per-month Revenue drill-down (v1.20.0; FR-95).** When a payload's `month.revenueItems` is non-empty, the Revenue cell MUST be a focusable button (`role="button"`, `tabindex="0"`, underline-dotted affordance, hover/focus accent). Clicking it MUST open `#deliveryRevenueMonthModal` with title `Revenue items — <Month Label>`, subtitle `<project name> · <N> milestone[s]` (with ` · Projected month` suffix when applicable), body rows of `Milestone · Amount · Date · Status` (status badge color via `milestoneStatusColor_`), and footer `Total $X · recognized $Y`. The same modal MUST open from the Chart view via the chart's `onClick` callback. Activity event `delivery_pnl_month_drilldown` MUST fire with label `monthKey=<key> · itemCount=<n>`.
- AC-51 **[Released]**: **Chart view + Copy CSV toolbar (v1.20.0; FR-96 / FR-97).** When a project is selected and the monthly grid has rows, the **P&L toolbar** MUST be visible: a Table / Chart `btn-group` (`fos-delivery-view-btn`) plus a `Copy CSV` button and a status span. Toggling to Chart MUST lazy-load Chart.js via `loadChartJs()` (only on first activation) and render a mixed chart with stacked Labor + Expenses bars and an overlaid Revenue line; projected months MUST use muted backgrounds per FR-96. Switching back to Table MUST NOT re-fetch. The Copy CSV button MUST serialize the visible months + Lifetime total to comma-separated text with columns Month, Revenue, Labor, Expenses, Total cost, Gross profit, Margin %, Projected, Out of range; success MUST flash `Copied N rows` in the status span for ~3 s, failure MUST flash `Copy failed`. Both controls MUST emit activity events (`delivery_pnl_view_toggle` with `view=<table|chart>`; `delivery_pnl_copy_csv` with `agreementId=<id> · rows=<n>`).
- AC-52 **[Released]**: **Search input (v1.20.0; FR-98).** The Active Projects header MUST render a search input above the table. Typing MUST filter visible rows by case-insensitive whitespace-tokenized substring match across Project + Customer (every token must match somewhere in the concatenated string); empty input MUST show all rows. The current term MUST persist in `fos_delivery_filters_v1` and survive reloads; the inline `×` clear button MUST appear when non-empty and clear the term + restore full list on click. The persistence write + `delivery_table_search` activity event MUST be debounced by ~400 ms; the activity log MUST fire only when the trimmed term is non-empty (`len=<trimmedLength>`). When all rows are filtered out, the table MUST hide and a dedicated `No projects match the current filter.` placeholder MUST surface (distinct from the cache-empty placeholder). Filtering MUST NOT clear an existing P&L card.
- AC-53 **[Released]**: **Chart / Table state interplay (v1.20.0; FR-97).** Selecting a project MUST destroy any existing Chart.js instance (`deliveryState.pnlChart.destroy()`) before rebuilding from the fresh payload. Deselecting a project (second-click on the selected row) MUST hide the toolbar AND the chart, destroy the chart instance, clear `deliveryState.lastPnlPayload` and `monthsByKey`, and revert the placeholder. Manual Refresh MUST destroy the chart in the same teardown path. Switching from Chart back to Table MUST NOT destroy the instance (so re-toggling Chart re-uses it). Chart.js MUST stay lazy-loaded — `Chart.min.js` MUST NOT be fetched on initial Delivery panel open or during Table-only navigation.
- AC-54 **[Released]**: **Delivery Phase C — pacing + signals + portfolio Sankey (v1.21.0; FR-99).** With a project selected and monthly P&L loaded, the P&L card MUST render the pacing strip when a duration window exists; the strip MUST hide when the user deselects or when duration dates cannot be parsed. The Delivery signals section MUST recompute whenever the Active Projects table re-renders (including search filter and sort) and MUST only read fields present on cached `projects[]` rows — it MUST NOT call `getDeliveryProjectMonthlyPnL` for portfolio triage. The portfolio Sankey MUST aggregate the same visible row set, reuse `loadD3Sankey()`, and pass a non-default `gradientPrefix` into the shared `drawSankey_` helper so gradient ids never collide with `#sankey-wrap`. Clicking a signal card MUST emit `delivery_attention_click` then select the project (`delivery_project_select` label includes `source=attention`).
- AC-55 **[Released]**: **Agreement Attention — delivery-risk rules (v1.21.0; FR-100).** The Agreement Dashboard payload's `alerts[]` MUST include any firing `pace-behind:`, `cost-exceeds-rec:`, or `low-rec-near-end:` cards alongside the legacy §6.1–§6.6 rules; severity ordering MUST remain Critical → Warning → Informational with the §6.7 all-clear fallback unchanged.
- AC-56 **[Released]**: **Labor hours dashboard (v1.22.0; FR-101).** The sidebar MUST render **Operations** as a labeled group with two child nav buttons — **Utilization** (`operations`) and **Labor hours** (`labor-hours`) — each with a distinct icon. `#panel-labor-hours` MUST ship a week control (`<input type="week">`), defaulting on first open to the last completed local Mon–Sun week; changing the week or Internal-labor filter MUST recompute tables without requiring a Utilization panel visit first. When the Utilization payload in memory or `sessionStorage` (`fos_utilization_dashboard_v1`) has `range.start` / `range.end` bracketing the selected week, opening or refreshing Labor hours for that week MUST NOT invoke `getUtilizationDashboardData` (verify via Execution transcript or Network). When the cache does not cover the week, the client MUST call `getUtilizationDashboardData(weekStartIso, weekEndIso)` and MUST NOT replace the wide Utilization cache with the narrow response. Tables MUST list only people with `> 0` hours in the week after filters, split into Over / Under / On target using `laborHours` targets from the payload, and MUST support sortable headers. Labor-hours–specific activity events are specified in **AC-59**; any other client `eventType` remains coercible per **FR-63**.
- AC-57 **[Released]**: **Labor hours Phase B (v1.23.0; FR-101 + FR-102).** The Labor hours KPI row MUST include a **Zero hours** count and a **Zero hours this week** section listing every `dimensions.persons[]` entry from the same utilization payload whose filtered week hours are below the configured on-target epsilon, excluding names matched by `excludedPersonSubstrings`. KPI tiles carrying `data-lh-scroll-to` MUST scroll the panel to the matching section on click and on **Enter** / **Space** when focused. Company-specific weekly targets from `companyTargetHoursMap` MUST override partner/default resolution for that `clockifyUserCompany` name (case-insensitive exact match).
- AC-59 **[Released]**: **Labor hours Phase C + D (v1.24.0; FR-103).** The Over / Under / On-target tables MUST render each **Projects** cell as a capped stack of native `<details>` / `<summary>` blocks — one per project (sorted by hours descending) showing project name, hours, and **% of that person’s week**, with nested task lines (from normalized `task` on labor rows) each labeled with hours and % of week; lists MUST truncate with **+N more projects** / **+N more tasks** when exceeding the shipped caps. The toolbar MUST expose **Expand projects** / **Collapse projects** controls that toggle every project `<details>` in `#panel-labor-hours`, a **Copy CSV** button that writes a comma-separated export (Over / Under / On rows plus a Zero roster block) to the clipboard with success/failure feedback, and MUST emit **`labor_hours_export`** (label includes `rows=` + ISO week value). Week changes MUST emit **`labor_hours_week_change`**; the panel **Refresh** button MUST emit **`labor_hours_refresh`** (instead of the generic `refresh` type). KPI navigation and table sort events continue to emit **`labor_hours_kpi_nav`** and **`labor_hours_sort`**. The server whitelist in `src/userActivityLog.js` MUST recognize those five `eventType` strings verbatim. **Print:** `@media print` rules MUST hide `.fos-sidebar` and `header.fos-topbar`; `beforeprint` / `afterprint` handlers MUST temporarily open project `<details>` so task lines appear on paper, then restore prior open state.
- AC-27 **[Released]**: **Loading overlay + sticky panel render (v1.13.1; spinner doubled + viewport-centered v1.14.2).** While any `google.script.run.getAgreementDashboardData` or `google.script.run.getUtilizationDashboardData` call is in flight (initial load, manual Refresh, range change, or background stale-refresh), the affected dashboard panel MUST render a **semi-transparent overlay** (`.fos-loading-overlay`) anchored inside its `.fos-agreement-inner` container, displaying a viewport-centered animated spinner (`.fos-loading-spinner`) and an uppercase "Loading data…" label (`.fos-loading-label`). As of **v1.14.2** the spinner MUST be **96×96 px** with an **8 px** border (twice the v1.13.1 size), and BOTH the spinner and label MUST use `position: fixed` with the offsets `top: calc(50% - 48px); left: calc(50% - 48px)` (spinner) and `top: calc(50% + 64px); left: 50%; transform: translateX(-50%)` (label) so they remain at the **viewport** center regardless of scroll position inside the dashboard — the panel-scoped overlay backdrop (blur + 55%-alpha navy) remains anchored to `.fos-agreement-inner`. The overlay MUST be toggled on at the entry to `fetchAgreementFromServer()` / `fetchUtilizationFromServer()` and off in both the success and failure handlers; the sidebar and topbar MUST stay interactive while the overlay is shown. Users with `prefers-reduced-motion: reduce` MUST see a static (non-spinning) progress indicator. Separately, navigating away from `#panel-agreement-dashboard` or `#panel-operations` and back MUST NOT re-execute the panel's `applyPayload()` (and therefore MUST NOT destroy + rebuild Chart.js / D3 / Detail-Table state) when the cache's `fetchedAt` matches the timestamp the DOM currently reflects — the panel-switch handler compares `agreementRenderState.lastRenderedFetchedAt` / `utilRenderState.lastRenderedFetchedAt` against the cache and skips the re-render on match. The Stale badge + background fetch logic (FR-56b / FR-73) MUST continue to fire when the cache is stale even if the re-render itself is skipped.

## 12) Open Questions (for product refinement)

- Which **v1 KPI set** is mandatory for launch (exact list, owners, and source per metric)?
- **Single deployment** vs separate dev/prod Apps Script projects and Web App URLs?
- Preferred **metric catalog** format: Sheets-only v1 vs JSON in Drive vs hybrid?
- **Role model**: one dashboard for all internal viewers vs role-specific layouts? (**Partially answered** for v1: role and team come from the users sheet; layout rules TBD per feature 002 delivery.)

## 13) Change Log

| Date | Version | Change Summary | Author |
| --- | --- | --- | --- |
| 2026-05-13 | 1.26.0 | **Monthly Revenue Review — Phase C + D (tables, CSV, print, drill-down).** **Client (`src/DashboardShell.html`):** prior + current **milestone billing** tables (excl. Not Scheduled), **variance & schedule** table (amount or past-target-not-invoiced in prior/current target months), **agreement portfolio**, **revenue by customer**, **overdue** table; client-side **sort** (persisted `sessionStorage` `fos_rr_table_sort_v1`); **Copy CSV** per section + **Copy all**; **print** expands `<details>` (customer → agreement) before print; **milestone detail** hierarchy; **Variances** KPI jumps to variance table (`revenue_review_kpi_nav`). **Server (`src/userActivityLog.js`):** whitelist **`revenue_review_sort`**, **`revenue_review_export`**, **`revenue_review_expand`**, **`revenue_review_kpi_nav`**, **`revenue_review_print`**. MINOR bump — `FOS_PRD_VERSION` and headers → **1.26.0**. | Cursor |
| 2026-05-13 | 1.25.0 | **Monthly Revenue Review — Phase A + B (Delivery nav group + KPIs).** **Navigation (`src/Code.js`):** **Delivery** becomes a sidebar **group** with children **Projects & P&L** (`delivery`) and **Revenue review** (`revenue-review`). **Client (`src/DashboardShell.html`):** new `#panel-revenue-review` (dark `.fos-agreement-root` chrome) reads the same **`fos_agreement_dashboard_v2`** cache + TTL as the Agreement Dashboard; **Refresh** calls `getAgreementDashboardData()` with a revenue-review loading path; six executive KPI cards, **Agreement expiry** strip (excludes Contract Complete), and **Future revenue pre-recognition** banner from normalized agreements + `revenueItemsByAgreement` (Internal + name-contains-`test` exclusions). **Server (`src/userActivityLog.js`):** whitelist **`revenue_review_refresh`**. MINOR bump — `FOS_PRD_VERSION` and all `src/*` headers → **1.25.0**. | Cursor |
| 2026-05-13 | 1.24.0 | **Labor hours Phase C + D — project/task `<details>`, Copy CSV, print, activity whitelist.** Client (`src/DashboardShell.html`): capped expandable project + task breakdown in the Projects column; expand/collapse-all; **Copy CSV** + `labor_hours_export`; `labor_hours_week_change` + `labor_hours_refresh`; print CSS + `beforeprint` / `afterprint` detail expansion. Server (`src/userActivityLog.js`): whitelist the five `labor_hours_*` event types. **Docs:** **FR-103**, **AC-59**; MINOR bump — `FOS_PRD_VERSION` and all `src/*` headers → **1.24.0**. | Cursor |
| 2026-05-13 | 1.23.0 | **Labor hours Phase B — zero roster, KPI jump, company map, exclusions.** `#panel-labor-hours` adds a **Zero hours** KPI + chip list sourced from `dimensions.persons` vs filtered week rows; KPI tiles scroll to Over / Under / On / Zero sections (click + keyboard). Server `getLaborHoursConfig_()` gains optional **`LABOR_HOURS_COMPANY_TARGETS_JSON`** and **`LABOR_HOURS_EXCLUDED_PERSON_SUBSTRINGS`** (surfaced on `laborHours`). Client: `src/DashboardShell.html` + `src/utilizationThresholds.js`. **Docs:** FR-101 extended, new FR-102 + AC-57; MINOR bump — `FOS_PRD_VERSION` and all `src/*` headers → **1.23.0**. | Cursor |
| 2026-05-13 | 1.22.0 | **Labor Hours dashboard (Operations) + `laborHours` payload.** Nested sidebar **Operations** group with **Utilization** (`operations`) and **Labor hours** (`labor-hours`); new `#panel-labor-hours` with ISO week picker (default last completed Mon–Sun), KPI strip, Over / Under / On-target sortable tables, top-projects column, shared Internal-labor filter with Utilization, loading overlay reuse, and cache-first week slicing when `fos_utilization_dashboard_v1` range covers the selected week. Server: `getLaborHoursConfig_()` in `src/utilizationThresholds.js` (Script Properties `LABOR_HOURS_DEFAULT_WEEKLY_TARGET`, `LABOR_HOURS_PARTNER_WEEKLY_TARGET`, `LABOR_HOURS_PARTNER_COMPANY_SUBSTRINGS`); `getUtilizationDashboardData` attaches `laborHours` on success and error payloads. Client: `src/DashboardShell.html`. **Docs:** FR-101, AC-56; MINOR bump — `FOS_PRD_VERSION` and all `src/*` headers → **1.22.0**. | Cursor |
| 2026-05-13 | 1.21.1 | **Utilization Alerts — default collapsed.** On the Operations panel, kind-based Utilization Alert groups (Under-utilized / Over-allocated / Stale approvals) now render **collapsed** on initial load and after every `applyUtilPayload()` refresh; operators expand a group by clicking its header. **Client:** `buildAlertGroupCard_` in `src/DashboardShell.html` treats only `utilState.alerts.expanded[kind] === true` as open (removes the prior auto-expand rules for Critical-only or single-alert groups). **Docs:** AC-38 revised; PATCH bump — `FOS_PRD_VERSION` and all `src/*` + feature doc PRD headers → **1.21.1**. | Cursor |
| 2026-05-13 | 1.21.0 | **Delivery Dashboard — Phase C (client) + Agreement Attention delivery-risk rules.** **Delivery (`src/DashboardShell.html`):** adds a **pacing strip** on the P&L card (duration timeline vs recognition %, linear expected recognition from contract value × elapsed share, Δ vs linear, trailing 3-mo average from cached months); a **Delivery signals** attention-style strip above the Active Projects table driven only from visible `projects[]` rows (negative margin, cost pressure, completion lag, low recognition near end); a **portfolio margin-flow Sankey** (recognized → labor / ODC / gross margin) sharing `loadD3Sankey` + `drawSankey_` with a distinct gradient prefix; `delivery_attention_click` activity logging; `delivery_project_select` labels extended with `· source=<table|click|attention|restore>`. **Agreement (`src/agreementAlerts.js`):** `evaluateAlerts_()` adds `pace-behind`, `cost-exceeds-rec`, and `low-rec-near-end` rules per FR-100. **Docs:** new FR-99 / FR-100, §9.6, AC-54 / AC-55; feature **006** Phase C row marked shipped. MINOR bump — new user-visible surfaces + new attention ids; no breaking API changes. Version + all `src/*` + feature headers → **1.21.0**. | Cursor |
| 2026-05-13 | 1.20.0 | **Delivery Dashboard — Phase B delivered.** Extends the Phase A P&L card with chart-view, drill-down, projected-month support, CSV copy, and an Active Projects search input. **Server work** (`src/deliveryDashboard.js`): (1) the per-project Revenue Items fetch drops the `Revenue Recognized = true` filter and returns every milestone for the agreement with `name`, `recognized`, and workflow `state` per row so the FR-95 drill-down has full fidelity; (2) the `DELIVERY_PNL_INCLUDE_PROJECTED_ODC` default flips from `false` to `true` so `Status = Projected` ODC rows participate in the monthly aggregation by default (operators may opt back to actual-only by setting it `false`); (3) `buildMonthlyPnL_` now emits two new per-month fields — `projected: bool` (true when the month key is later than the current UTC `yyyy-mm`) and `revenueItems: [{ id, name, amount, recognized, targetDate, actualDate, state }]` — so the chart, the projected-month pill, and the drill-down modal all consume the same payload; (4) `DELIVERY_PNL_CACHE_SCHEMA_VERSION_` bumps to **2** and the client cache key suffix moves from `_v1` to `_v2` so v1.19.0 caches are invalidated cleanly on upgrade. **Client work** (`src/DashboardShell.html`): new **P&L toolbar** above the monthly grid containing a Table / Chart `btn-group` toggle and a **Copy CSV** button (with a status span). The **Chart view** lazy-loads `Chart.js` via the existing `loadChartJs()` helper and renders a mixed chart — stacked Labor + Expenses bars with an overlaid Revenue line; projected months use muted backgrounds (`rgba(252,92,101,0.32)` Labor, `rgba(249,199,79,0.38)` Expenses, `rgba(67,214,186,0.55)` Revenue point) so the actual-vs-projected split pops visually. Clicking a bar / data-point in the chart opens the same drill-down modal as the table view. **Projected month pill + row class** added to the table view (`.fos-delivery-projected-pill` + `tr.is-projected`); each month row whose `projected === true` carries the pill on the month label, the `is-projected` muted treatment, and a left accent stripe matching the active-row affordance. **Per-month Revenue drill-down** (`#deliveryRevenueMonthModal`) — every Revenue cell becomes a focusable button when `month.revenueItems.length > 0`; clicking opens a Bootstrap modal titled `Revenue items — <Month Label>` with rows of `Milestone · Amount · Date · Status` (reusing `milestoneStatusColor_` for the status pill palette) and a `Total $X · recognized $Y` footer; the same modal opens from the chart's `onClick`. **Copy CSV** serializes the visible months + Lifetime total row to a comma-separated payload (columns: `Month, Revenue, Labor, Expenses, Total cost, Gross profit, Margin %, Projected, Out of range`) and writes it to the clipboard via `navigator.clipboard.writeText` with a textarea + `execCommand('copy')` fallback; status flashes in the toolbar for ~3 s. **Active Projects search input** added to the table header — case-insensitive whitespace-tokenized substring filter across Project name + Customer name; the term persists in `fos_delivery_filters_v1` alongside sort + selection + view; debounced 400 ms; inline `×` clear button surfaces when non-empty; a dedicated `No projects match the current filter.` placeholder shows when filtering hides every row. **Persistence:** `deliveryState` extended with `searchTerm`, `pnlView`, `lastPnlPayload`, `monthsByKey`, `pnlChart`, `csvStatusTimer`; the filters blob picks up `searchTerm` + `pnlView` (additive — `schemaVersion: 1` stays). **Activity logging:** four new event types — `delivery_pnl_view_toggle` (`view=<table|chart>`), `delivery_pnl_month_drilldown` (`monthKey=<key> · itemCount=<n>`), `delivery_pnl_copy_csv` (`agreementId=<id> · rows=<n>`), `delivery_table_search` (`len=<trimmedLength>`; fires only on non-empty terms). **Docs:** new **FR-94** (projected months + cache v2), **FR-95** (per-month Revenue drill-down), **FR-96** (chart + Copy CSV toolbar), **FR-97** (chart / table state interplay + lazy Chart.js), **FR-98** (search input). New **AC-49..AC-53** mirror the FRs with verification-level detail. New **§9.5** "Components (Phase B — v1.20.0)" added beneath the existing Phase A subsection. `DELIVERY_PNL_INCLUDE_PROJECTED_ODC` default in §9.4 flipped to `true`. README's Delivery row updated with Phase B summary. MINOR bump per `.cursor/rules/google-apps-script-core.mdc` — net new user-visible surfaces (chart view, drill-down modal, CSV copy, search input, projected pills) + four new activity-event types + server payload extension (additive month fields); no breaking changes to other dashboards. Cache key suffix bump (`_v1` → `_v2`) invalidates Phase A monthly P&L caches on upgrade — the projects-list cache (`_v1`) is unchanged. Version and `src/` headers → **1.20.0**. | Cursor |
| 2026-05-13 | 1.19.0 | **Delivery Dashboard — Phase A delivered.** Flips the **Delivery** nav entry (route id `delivery`, panel `#panel-delivery`) from the "coming soon" placeholder to a working dashboard backed by Fibery. **Top of panel:** an **Active Projects** table — Project · Customer · Type · Status · Contract value · Recognized · % Complete (colored progress bar via the new §D.11 four-bucket map: Under `< 25%` blue, Building `25–75%` teal, On-track `75–100%` green, Over `> 100%` orange) · Margin (target-variance dot via the new §D.10 three-bucket map: green ≥ target, amber within 5 pts below, red further below, neutral gray when either value is null). The list reuses `getAgreementDashboardData()` server-side — **zero extra Fibery queries** to render — and re-projects the agreements into Delivery rows with the bucket coloring + total cost + variance precomputed. **Bottom of panel:** a **Profit & Loss** card seeded lazily on row click. The **KPI strip** (Contract value · Revenue recognized · Total cost · Gross profit · Margin) renders synchronously from the cached row so the user sees the lifetime view immediately. The **Monthly P&L grid** is fetched on demand via the new `getDeliveryProjectMonthlyPnL(agreementId)` endpoint, which issues three small per-agreement Fibery queries — `Labor Costs` (with `Cost` + `Start Date Time`), `Other Direct Costs` (with `Amount` + `Date` + `Status`; filtered to `Actual` unless an opt-in flag is set), and `Revenue Item` (filtered to `Revenue Recognized = true`, picking up `Actual Amount` / `Actual Date` with fallback to Target) — aggregates them into one row per calendar month spanning the project Duration, and applies `marginVarianceBucket_` to each month's Margin % cell against the agreement's Target Margin for the green / amber / red coloring (decision M.7). Months with stray activity **outside** the Duration window are emitted with an italic `(OOR)` marker (decision M.3); zero-activity months **inside** Duration render muted (decision M.4). **Operational signals:** a `Partial data — capped at <N> rows` badge surfaces when the Labor Cost row cap is hit (decision M.6); a §M.9 reconciliation caption surfaces under the grid when summed monthlies diverge from the agreement's lifetime fields by more than 5% (decision M.5). **Caching:** dual `sessionStorage` strategy — projects list under `fos_delivery_dashboard_v1` and per-project P&L under `fos_delivery_pnl_<agreementId>_v1`, both `cacheSchemaVersion: 1`. A single TTL preference (`fos_delivery_dashboard_ttl_minutes_v1`, default 10 min, options 5 / 10 / 30 / Off) drives staleness for both. Manual **Refresh** invalidates every `fos_delivery_pnl_*` entry alongside the projects-list cache. **Activity logging:** eight new event types — `delivery_panel_open`, `delivery_refresh`, `delivery_project_select`, `delivery_project_deselect`, `delivery_table_sort`, `delivery_pnl_fetch_start`, `delivery_pnl_fetch_done`, `delivery_pnl_fetch_error` — all routed to `delivery`. **Server work:** new module `src/deliveryDashboard.js` (two public endpoints + three Fibery fetchers + the monthly aggregation builder + two editor diagnostics `_diag_sampleDeliveryPayload()` / `_diag_sampleMonthlyPnL(id)`); `src/agreementThresholds.js` extended with the §D.10 + §D.11 bucket maps + their Script Property overrides (`DELIVERY_COMPLETION_UNDER_PCT` / `_BUILDING_PCT` / `_OVER_PCT` and `DELIVERY_MARGIN_VARIANCE_AMBER_PTS`). **Client work** (`src/DashboardShell.html`): new `#panel-delivery` markup with header / refresh row / Active Projects card / P&L card; new CSS scope for `.fos-delivery-table`, `.fos-delivery-progress-*`, `.fos-delivery-pnl-kpi-chip`, `.fos-delivery-monthly`, `.fos-delivery-pnl-loading` (scoped grid-only spinner overlay), and the `.fos-delivery-partial-badge` + `.fos-delivery-reconcile` accents; new module-scope `deliveryState` + cache helpers + delegated row click handler + sortable header wiring; the nav-click branch routes `delivery` to `showDelivery()` (no more coming-soon modal). New **§9** "Delivery Dashboard" section with the normative caching / branding / components / Script Properties contract (renumbered §9–§12 → §10–§13). New **FR-89** (panel activation + Active Projects table + sort + caches + completion/margin buckets), **FR-90** (row selection + KPI strip), **FR-91** (lazy monthly P&L fetch + grid display contract), **FR-92** (partial badge + lifetime total + reconciliation caption + error handling), **FR-93** (eight new activity events + Refresh cache invalidation + two diagnostics). New **AC-43** through **AC-48** mirror the FRs with verification-level detail. README's "Other routes" row replaced with a full **Delivery (Delivery Dashboard)** row. MINOR bump per `.cursor/rules/google-apps-script-core.mdc` — net new user-visible surface (third dashboard panel) + new server endpoint + new event vocabulary; no breaking changes to the other two dashboards. Version and `src/` headers → **1.19.0**. | Cursor |
| 2026-05-13 | 1.18.0 | **Heatmap-cell modal · Agreement milestones modal · Persons alpha-sort · Fibery link tolerance.** Four targeted improvements across both dashboards based on hands-on feedback. **(1) Heatmap cell → modal (FR-85).** Replaces the v1.14.0 click behavior (pin Person + switch range to clicked week + server fetch) with a read-only modal that lists the labor entries contributing to the clicked `(person, week)` cell. The prior drill was disruptive — every cell click invalidated the cache and forced a fresh Fibery round-trip even when the user just wanted to know "which rows produced this color?" The new modal answers exactly that question against the already-loaded payload. **(2) Agreement → milestones modal (FR-86).** Each row in the Agreement Dashboard's Financial performance table is now click + keyboard activatable; activation opens a modal listing the agreement's Revenue Items as Milestone · Target amount · Target date · Invoice status. Data is served from a new `payload.revenueItemsByAgreement` map built by re-keying the existing historical + future revenue item arrays (zero new Fibery fetches; zero new queries). **(3) Persons dropdown alpha sort (FR-87).** The Operations panel's **Persons** multi-select used to inherit the server's hours-desc order, which is great for top-contributor triage but useless when you're looking for a specific teammate by name. Now sorted alphabetically in the menu only — heatmap row order and KPI computation continue to read the unmutated server order. **(4) Fibery link tolerance (FR-88).** Hardens the v1.15.0 access plumbing against operator typos and surfaces self-diagnostic data. `findHeaderIndex_` falls back to a loose lowercase + strip-non-alphanumeric match after the strict match misses, so `Fibery Access`, `Fibery_Access`, `fibery-access`, and `fiberyAccess` all resolve correctly to the `fibery_access` column. `getFiberyDeepLinkConfig_` now strips scheme + trailing slash from `FIBERY_HOST` the same way `fiberyClient.js` does, defusing the common "https:// prefix in the property" mistake. The client `getFiberyLaborCostUrl_` logs a `[FOS] Open-in-Fibery hidden: <reason>` warning to the browser console naming the specific failure branch so operators can devtools-diagnose without re-reading the source. A new `_diag_fiberyAccess()` editor helper returns `{ email, authOk, fiberyAccess, deepLinkConfig, sampleUrl, notes[] }` for support triage. **Activity logging delta:** new `util_heatmap_cell_click` and `agreement_milestones_open` events; `util_heatmap_drill` retired. Cache schema **unchanged** (v2 / v1 stay). | Code agent |
| 2026-05-13 | 1.17.0 | **Utilization Alerts grouped by alert kind + Collapse all affordance.** Operator feedback on the v1.16.0 by-person grouping: managers triage **a class of issue at a time** (review every under-utilized contributor in one pass, then every over-allocation, then the stale-approval backlog) rather than chase one person across multiple kinds. v1.17.0 flips the grouping axis from `target.person` to the alert **`kind`** (Under-utilized · Over-allocated · Stale approvals) — server payload contract preserved verbatim; presentation transform only. **Standalone-kind set** unchanged: `stale_approval_rollup` and `all_clear` stay flat above the grouped section because they're panel-level messages with nothing to "expand". **Group key:** the `kind` field on each alert; unknown values fall back to a humanized label (`priority_review` → "Priority review") so a forward-compatible server can introduce new rules without a client release. **Group state:** `utilState.alerts.expanded` is now keyed by `kind` instead of `personKey`; the wrapper carries `data-kind="<kind>"` so the Collapse all handler can sync without re-deriving from the DOM-safe slug. **Default expand semantics** unchanged: groups with at least one Critical alert or with exactly one alert are open; others are collapsed. **New affordance: Collapse all button.** The panel header surfaces a right-aligned **Collapse all** pill (reuses the `.fos-util-clear-btn` chrome from Pending Approvals' "Show all") that closes every kind-based group in one click — sets `is-open` off, syncs `aria-expanded="false"` on each header, writes `false` into `utilState.alerts.expanded[<kind>]` per group so the override survives filter-change re-renders until the next `applyUtilPayload()` clears the slate. The button auto-hides when there are no groups (panel-flat) AND when every group is already collapsed; visibility is recomputed on every render AND after every individual toggle. **New activity-log event** `util_alert_collapse_all` (route = `operations`, label = `groups=<count-collapsed>`). **Revised activity-log event** `util_alert_group_toggle` label format: `kind=<kind> · expand|collapse` (was `personKey=<key> · expand|collapse`). FR-80's v1.16.0 client-rendering clause patched in place to document the new axis + the Collapse all contract; **AC-38** patched (versioned as "v1.16.0; grouping axis revised v1.17.0") to describe the new key + label format; **new AC-39** documents the Collapse all button's full contract (markup, activity event, visibility rules, no-effect on standalone cards, non-destructive write into `utilState.alerts.expanded`). Helpers added: `kindLabel_(kind)` for friendly labels, `isStandaloneAlertKind_(kind)` for partition, `slugForDomId_(key)` (generalized rename of v1.16.0's `encodePersonKeyForId_`), `collapseAllUtilAlertGroups_()`, `updateCollapseAllButtonVisibility_()`. Helper removed: `extractPersonNameFromAlert_` (only the v1.16.0 by-person axis needed title parsing). Cache schema unchanged (still **v2**); no Script Properties added. MINOR bump per `.cursor/rules/google-apps-script-core.mdc` — net new user-visible affordance (Collapse all button) + new activity event vocabulary + visible behavior change on the grouping axis. Version and `src/` headers → **1.17.0**. | Cursor |
| 2026-05-12 | 1.16.0 | **Person-grouped Utilization Alerts.** Reorganizes the Operations panel's **Utilization Alerts** widget from a flat severity-sorted list into a **collapsible per-person grouping**, eliminating the long-standing scan-fatigue when one contributor accumulates 3–5 simultaneous issues (over-allocated + stale approvals across multiple rows is now one card to triage, not five). Client-only change — the server `alerts[]` payload contract is **unchanged**, the cache schema stays at **v2**, no Script Properties added. **Markup:** `renderUtilizationAlerts(alerts)` now partitions the incoming list into (a) **standalone** cards for alerts whose `target` lacks `person` (the `stale_approval_rollup` and the `all_clear` info card — these stay flat above the grouped section so panel-level messages remain unmissable) and (b) **grouped** cards keyed by `target.person`. Group ordering: worst-severity first → alert-count desc → person name asc; within a group, the server's Critical → Warning → Info order is preserved. The group wrapper carries `.sev-<level>` matching the worst alert in the group, which drives the border-left strip, the header's emoji severity icon, and a muted count pill (`<N> alerts`). **Default expand state:** Critical groups are expanded; single-alert groups are expanded (collapse buys nothing when there's one item); everything else is collapsed. Manual toggles persist in the new `utilState.alerts.expanded` map (keyed by `personKey`) for the lifetime of the current payload, and are cleared inside `applyUtilPayload()` so each new payload starts with the smart defaults. **Interaction:** header click OR `Enter` / `Space` keyboard activation toggles the group and emits a new **`util_alert_group_toggle`** activity-log event (route = `operations`, label = `personKey=<key> · expand|collapse`). Individual alert clicks inside an expanded group call `stopPropagation()` so they reach `handleUtilAlertClick_` (preserved drawer-open / person-filter / scroll-to-Pending paths) without also toggling the group. **Accessibility:** header is `role="button"`, `tabindex="0"`, with `aria-expanded` synchronized to the visible state and `aria-controls` referencing a stable id on the children container; chevron rotation honors `prefers-reduced-motion: reduce`. **CSS:** new `.fos-attention-group` / `-header` / `-chevron` / `-name` / `-count` / `-sev-icon` / `-children` classes added under the existing `.fos-attention-item` palette, with `.is-open` modifier driving both chevron rotation and child-list visibility (the children container uses `display: none` rather than `visibility: hidden` so collapsed groups don't take vertical space). Children inside a group lose the heavy left border so the group's severity strip remains the visual anchor; consecutive children are separated by a 1px hairline. **FR-80** extended with a v1.16.0 client-rendering clause (server contract preserved verbatim); new **AC-38** documents the grouping rules, severity rollup, default expand semantics, sort order, accessibility contract, and activity event. **MINOR bump** per `.cursor/rules/google-apps-script-core.mdc` — net new user-visible behavior + new activity-log event vocabulary; no breaking changes (existing event types and the server payload contract are unchanged). Version and `src/` headers → **1.16.0**. | Cursor |
| 2026-05-12 | 1.15.0 | **Fibery deep-link composition + per-user Fibery Access gate.** Fixes a long-standing bug in the Operations row-detail drawer ("Open in Fibery →" anchor) and gates it behind a per-user flag. **Bug:** the hard-coded URL `https://harpinai.fibery.io/Agreement-Management/Labor-Cost-{publicId}` was wrong on three axes — wrong workspace host (`harpinai` vs the real `harpin-ai`), wrong entity-type path (`Agreement-Management/Labor-Cost-` vs `/Agreement_Management/Labor_Costs/`), and wrong slug rule (publicId only vs Fibery's `{name-with-spaces-as-hyphens}-{publicId}` convention). Real URLs look like `https://harpin-ai.fibery.io/Agreement_Management/Labor_Costs/2026-03-20---Alex-Anakin---0.5-hrs-167141`. **Fix (FR-84):** new server module `src/fiberyDeepLinks.js` exposes `getFiberyDeepLinkConfig_()` returning `{ scheme, host, laborCostPathTemplate }` from Script Properties `FIBERY_PUBLIC_SCHEME` (default `https`), `FIBERY_DEEP_LINK_HOST` (falls back to `FIBERY_HOST`), and `FIBERY_LABOR_COST_PATH_TEMPLATE` (default `/Agreement_Management/Labor_Costs/{slug}-{publicId}`). The config block is attached to the navigation payload only when the signed-in user is gated `true`. The client composes the URL via `scheme + '://' + host + template.replace('{slug}', row.name.replace(/\s/g, '-')).replace('{publicId}', row.publicId)`. The previous hard-coded URL string is purged from the codebase. **Per-user gate (FR-83):** new optional column **`fibery_access`** on the Users sheet (header overridable via `AUTH_COL_FIBERY_ACCESS`). Recognized truthy: `TRUE` / `true` / `yes` / `y` / `1` / Sheets boolean `true`; everything else (blank, missing column, garbage) **denies by default**. `getAuthorizationForActiveUser_()` now attaches `fiberyAccess: boolean` to the success envelope; `buildNavigationModel_()` surfaces it as `nav.fiberyAccess` and **only** attaches `nav.fibery` (the deep-link config) when the flag is `true` — defense in depth: gated users never receive the host or template through any channel. Client `renderDrawerBody_()` hides the **Open in Fibery →** anchor AND collapses the surrounding `.fos-util-drawer-footer` (`d-none`) whenever the gate is closed, the config is missing, or the row lacks `publicId` / `name`. New activity-log event type **`util_drawer_fibery_click`** (route = `operations`, label = `rowId=<id>`). New **FR-83** (per-user gate), **FR-84** (server-supplied deep-link template); **FR-82** patched to remove the hard-coded URL claim; new **AC-36** (gate semantics across all access-table cases), **AC-37** (URL composition + example + graceful degradation when host is unset); **AC-31** patched to reference the new gate. `docs/features/002-spreadsheet-user-authorization.md` extended with the `fibery_access` column contract + truthy-value table + deny-by-default rule. `docs/features/005-utilization-management-dashboard.md` extended with the new deep-link Script Properties + the gate's effect on the drawer footer. New editor diagnostic `_diag_fiberyDeepLinkSample()` in `src/fiberyDeepLinks.js`. **MINOR bump** per `.cursor/rules/google-apps-script-core.mdc` — net new user-visible surface (per-user gate, three new Script Properties, new event type) and a behavior change on the drawer anchor; cache schema unchanged (still **v2**); no breaking changes to other dashboards. Version and `src/` headers → **1.15.0**. | Cursor |
| 2026-05-12 | 1.14.2 | **Loading spinner doubled + viewport-fixed.** Cross-dashboard UX polish on top of v1.13.1: the `.fos-loading-spinner` inside `.fos-loading-overlay` is now **96×96 px with an 8 px border** (twice the previous 48×48 / 4 px), and both the spinner and its "Loading data…" label use **`position: fixed`** with viewport-centered offsets (`top: calc(50% - 48px); left: calc(50% - 48px)` for the spinner — using calc offsets instead of `transform: translate(-50%, -50%)` so the rotate animation has the `transform` channel to itself; `top: calc(50% + 64px); left: 50%; transform: translateX(-50%)` for the label). Net effect: the indicator stays at the visible viewport center no matter how far the user has scrolled down a long Operations / Agreement Dashboard view (previously the spinner sat at the panel's vertical mid-point, frequently above the fold during a refresh from the Detail Table). The dark / blurred backdrop remains panel-scoped so the sidebar + topbar stay legible. Label upgraded from `var(--text-muted)` to `var(--text)` with a subtle text-shadow + bumped to `font-weight: 700` for legibility on the heavier blur. **FR-54**, **FR-73**, and **AC-27** all updated to record the spinner/label sizing + the `position: fixed` viewport-centering contract. PATCH bump per `.cursor/rules/google-apps-script-core.mdc` — no new feature surface; the existing loading-overlay contract is preserved, only the spinner geometry + anchor point change. Version and `src/` headers → **1.14.2**. | Cursor |
| 2026-05-12 | 1.14.1 | **Phase C pending-approval correctness fix + stale-approval cap.** Patches a false-positive bug introduced in v1.14.0 that flagged virtually every labor row as `isPending` (observed in the field: 7,497 alerts on the Utilization Alerts panel, most rows that were actually `Approval = Approved` showing as "Pending approval N days old"). Root cause: `isPendingApproval_` in `src/fiberyUtilizationDashboard.js` checked `Approval` first and then fell through to `Time Entry Status`; rows with an explicit `Approved` value but a blank `Time Entry Status` (common for Clockify-synced rows where the status field never gets populated) hit the second branch's `t === ''` clause and returned `true`. **Fix:** explicit `Approval = 'approved'` now short-circuits as **never pending**, explicit `Approval ∈ {unapproved, pending}` returns pending, and a blank `Time Entry Status` is no longer pending on its own — fallback only fires when `Time Entry Status ∈ {not_submitted, pending}` *and* approval is missing/unknown. Separately, the Alerts panel now **caps individual `stale_approval` cards at the 20 oldest** and emits one consolidated `stale_approval_rollup` Warning card for the remainder; clicking the rollup flips the Pending Approvals widget into **Show all** mode and scrolls to it — preserving the full drill path while keeping the Alerts surface scannable even when a multi-thousand-row approval backlog exists. New constant `UTIL_ALERT_STALE_CAP_ = 20` in `src/utilizationAlerts.js`. New editor diagnostic `_diag_sampleUtilizationPending()` dumps the Approval × Time-Entry-Status distribution + the count of rows the predicate currently flags as pending, for verifying the fix against live data. **FR-80** extended to document the corrected pending semantics + the cap + the rollup contract; **AC-28** extended; PATCH bump per `.cursor/rules/google-apps-script-core.mdc` — no new feature surface; correctness fix on existing documented behavior. Cache schema version stays at **2** (shape unchanged; existing cached payloads from the buggy build will simply produce corrected `isPending` values on the next refresh). Version and `src/` headers → **1.14.1**. | Cursor |
| 2026-05-12 | 1.14.0 | **Utilization Management Dashboard — Phase C delivered.** Adds **Utilization Alerts**, an interactive **Person × Week heatmap** with a heatmap-local Role filter, a **Pending Approvals widget**, and a **row-detail drawer** to `#panel-operations`. **Server work:** new `src/utilizationAlerts.js` evaluates three rule families — Under-utilized (Warning; mean util% across the last 3 complete weeks `< thresholds.underPercent`, PTO weeks excluded), Over-allocated (Critical; any 2 consecutive complete weeks `> thresholds.overPercent`, first triggering pair per person only), and Stale approval (Warning ≥ 7 days, Critical ≥ 14 days). Output is severity-sorted Critical → Warning → Informational with the all-clear card as the fallback. `src/fiberyUtilizationDashboard.js` adds `buildByPersonWeek_(rows, range, thresholds)` which pro-rates partial-week capacity by `(daysInRangeInWeek / 7)` and wires both `alerts[]` and `aggregates.byPersonWeek` into the public payload; also adds `_diag_sampleUtilizationAlerts()` editor helper. `src/utilizationThresholds.js` adds `UTILIZATION_HEATMAP_TOP_N_PERSONS` (default 30), `UTILIZATION_STALE_APPROVAL_WARN_DAYS` (default 7), and `UTILIZATION_STALE_APPROVAL_CRIT_DAYS` (default 14) Script Properties (resolver enforces `crit > warn`). **Client cache schema bumped 1 → 2**; clients carrying v1 silently drop and refetch. **Client work** (`src/DashboardShell.html`): new sections **Utilization alerts** (above KPI strip, reuses `.fos-attention-item` styling), **Utilization heatmap** (SVG grid; rows capped at 30; §U.10 bucket colors; diagonal-hatch overlay for partial weeks via SVG `<pattern>`; section-local **Role multi-select** distinct from the global Role filter), **Pending approvals** (capped at 50 with Show all toggle; amber/red age badges), and **Row-detail drawer** (off-canvas right, z-index 1100 / backdrop 1090, slide-in 0.22s suppressed under `prefers-reduced-motion: reduce`, dl-formatted full row + Open in Fibery deep link by `publicId`, closable via button / backdrop / Escape). Click flows: alert → drawer (when `target.rowId` set) or pin Person filter; heatmap cell → pin Person filter + switch range to clicked week + server fetch; pending row → drawer; Detail Table row → drawer (each `<tr>` carries `data-row-id`). New activity-log event types `util_alert_click`, `util_heatmap_drill`, `util_heatmap_role`, `util_pending_toggle`, `util_drawer_open`. **Docs:** new **FR-80** (alerts payload + cache schema bump), **FR-81** (heatmap contract incl. heatmap-local Role filter + 30-row cap), **FR-82** (Pending Approvals + drawer + activity logging); new **AC-28** (alerts panel), **AC-29** (heatmap), **AC-30** (Pending Approvals), **AC-31** (drawer), **AC-32** (cache schema bump v1 → v2), **AC-33** (Phase C activity logging), **AC-34** (configurable thresholds), **AC-35** (editor diagnostics). `docs/features/005-utilization-management-dashboard.md` Phase C status flipped to **Delivered v1.14.0**. MINOR bump per `.cursor/rules/google-apps-script-core.mdc` — net new user-visible surface (Alerts / Heatmap / Pending / Drawer + heatmap-local Role filter); no breaking changes to the Phase A + B contracts. Version and `src/` headers → **1.14.0**. | Cursor |
| 2026-05-12 | 1.13.1 | **Dashboard loading overlay + sticky panel render (UX polish on top of v1.13.0).** Adds a **semi-transparent loading overlay** (`.fos-loading-overlay`) anchored inside the `.fos-agreement-inner` container of **both** `#panel-agreement-dashboard` AND `#panel-operations`, with a centered animated spinner (`.fos-loading-spinner`) and an uppercase "Loading data…" label (`.fos-loading-label`); the overlay covers only the dashboard surface so the sidebar and topbar stay interactive throughout. Wired through two new helpers `setAgreementLoading(on)` / `setUtilLoading(on)` that are toggled on at the entry to `fetchAgreementFromServer()` / `fetchUtilizationFromServer()` and off in both the success and failure handlers — covering initial loads, manual Refresh, date-range changes, and background stale-refresh fetches. Honors `prefers-reduced-motion: reduce` by stopping the spinner animation. Separately, **panel-switch no longer forces a full re-render of cached data.** New module-scope render-state trackers `agreementRenderState.lastRenderedFetchedAt` and `utilRenderState.lastRenderedFetchedAt` are updated at the end of `applyAgreementPayload()` / `applyUtilPayload()`; `showAgreementDashboard()` and `showOperations()` now compare the cached payload's `fetchedAt` against this tracker and **skip** `applyAgreementPayload(cached)` / `applyUtilPayload(cached)` when they match — preserving Chart.js + D3-Sankey + Detail-Table state across nav toggles and eliminating the visible "reload flash." `applyStoredFilters_()` on the Util side moves inside the no-skip branch so a re-read of `localStorage` cannot desync the DOM from in-memory filter state. The Stale badge + background-refresh path (FR-56b / FR-73) continues to fire when the cache is stale even when the re-render is skipped. New **CSS** (`.fos-loading-overlay` + `.fos-loading-spinner` + `.fos-loading-label` + `@keyframes fos-spin` + `prefers-reduced-motion` fallback; `position: relative` added to `.fos-agreement-inner` to anchor the overlay). New **markup** (two `<div class="fos-loading-overlay d-none">` blocks at the top of each `.fos-agreement-inner`). New **els** entries `els.agreementLoading` / `els.utilLoading`. **FR-54** extended to document the agreement-side overlay contract; **FR-73** extended to document the util-side overlay contract + the cross-dashboard skip-rerender rule; new **AC-27** asserts the overlay behavior + the skip-rerender rule across both panels. PATCH bump per `.cursor/rules/google-apps-script-core.mdc` — no new feature surface; existing documented behavior (refresh / cache / filter contracts) is preserved; the change is a UX polish + perf improvement. Version and `src/` headers → **1.13.1**. | Cursor |
| 2026-05-12 | 1.13.0 | **Utilization Management Dashboard — Phase B delivered.** Adds cross-filter drill-down, the Detail Table, and persisted filter state to `#panel-operations`. Filter bar gains **Person** and **Role** multi-select dropdowns (populated from `dimensions.persons` / `dimensions.roles`); two new Chart.js v4 surfaces — **Hours by Role** (doughnut, colored from `UTILIZATION_ROLE_PALETTE_` via server-emitted `dimensions.roles[].color`) and **Hours by Person** (horizontal bar, top-`UTILIZATION_TOP_N_PERSONS` default 20, colored by primary role). Click-to-toggle wired on both new charts (Role slice → toggle Role filter; Person bar → toggle Person filter); Person + Role chips render in the chip row with `×` removal; **Clear filters** clears Person + Role alongside the other dimensions. **Detail Table** (§N.12) added below the charts: sortable columns Date · Person · Customer · Project · Role · Hours · Cost · Bill rate · Approval, paginated 100 rows / page, Approval rendered as a colored pill; reflects the active filter at all times. **Filter persistence** (`localStorage` key `fos_utilization_filters_v1`, `schemaVersion: 1`) stores customers / projects / persons / roles / billable / internalLabor across reloads — `range` is intentionally not persisted. New activity-log event type **`util_detail`** for table sort + pagination interactions. **Cosmetic — duplicate in-panel harpin logo removed from BOTH `#panel-agreement-dashboard` AND `#panel-operations` (cross-dashboard cleanup).** The sidebar `.fos-brand-logo` is now the single rendered brand surface; the `<img>` + `<div class="agreement-logo-sep">` blocks and the `.fos-agreement-root .agreement-logo-sep` CSS rule are all removed. Panel headings + subtitles remain unchanged. New **FR-77** (Person + Role filters + Role donut + Person bar), **FR-78** (Detail Table contract), **FR-79** (filter persistence); new **AC-24** (Person + Role cross-filter), **AC-25** (Detail Table sort + pagination), **AC-26** (filter persistence + single-brand-mark). FR-50 / §7.2 / §8.2 updated to reflect the single-source brand mark; **§8.3** renamed to "Components and charts (Phase A + Phase B)" with two new entries (§N.6 Role donut, §N.7 Person bar) and the Detail Table. Doc `docs/features/005-utilization-management-dashboard.md` Phase B status flipped to **Delivered v1.13.0**; `docs/features/003-…` adds a cosmetic cross-reference row + §7.1 page-header update. MINOR bump per `.cursor/rules/google-apps-script-core.mdc` — net new user-visible behavior (Person + Role filters, two new charts, sortable Detail Table, filter persistence). Version and `src/` headers → **1.13.0**. | Cursor |
| 2026-05-11 | 1.0 | Initial FOS Dashboard PRD; structure aligned with `docs/PRD.md`; all requirements tagged Backlog pending implementation. | Cursor |
| 2026-05-12 | 1.1 | Added spreadsheet **users** tab authorization (FR-05–FR-08a), not-authorized page requirement, server re-check rule, AC-09–AC-12, architecture update. | Cursor |
| 2026-05-12 | 1.2 | Implemented sheet auth (`authUsersSheet.js`), `NotAuthorized.html`, `doGet` gate, `getDashboardNavigation` re-check, role/team in nav payload, PRD version in UI; PRD headers in all `src` text files; FR-05–FR-08a, FR-51, AC-08–AC-12 marked **Released**. | Cursor |
| 2026-05-12 | 1.3 | Shell navigation: **Bootstrap Icons** on left menu items (FR-10a); **Settings** + gear replaces **Profile** with same placeholder behavior (FR-10b); **AC-13**; sync `FOS_PRD_VERSION` and all `src/` PRD headers to **1.3**. | Cursor |
| 2026-05-12 | 1.4 | Pulled **Fibery Agreement Management data model** from `docs/agreement-dashboard-prd-v2.md` §3 into new **`## 6) Fibery data model requirements`**; renumbered sections 6→10; **FR-25** (connector conformance); version and `src` headers → **1.4**. | Cursor |
| 2026-05-12 | 1.5 | **Agreement / Finance dashboard** contract: new **`## 7)`** (Fibery live + **no server datastore** for this slice + **client cache** + **§9.5–§9.7** branding); **FR-52–FR-56**, **NFR-07**, architecture note; renumbered sections 7→11; feature **`docs/features/003-agreement-dashboard-fibery-client-cache.md`**; **AC-14**; initial UI + `getAgreementDashboardData` stub; version and `src/` headers → **1.5**. | Cursor |
| 2026-05-11 | 1.6 | **User activity logging**: new **`§3.8`** with **FR-60–FR-66** describing the **`User Activity`** tab in the Users spreadsheet, Script Properties (`AUTH_USER_ACTIVITY_SHEET_NAME`, `USER_ACTIVITY_LOGGING_ENABLED`), event vocabulary (`page_load`, `nav_view`, `refresh`, `server_call`), `LockService` append semantics, and PII-bound payload; **NFR-08** activity-log privacy; **AC-15–AC-17**; architecture diagram updated; feature **`docs/features/004-user-activity-logging.md`**; implementation: new `src/userActivityLog.js` (`logUserActivity`, `recordPageLoad_`, `writeActivityRow_`, sanitizers), `doGet` page-load hook in `src/Code.js`, and `nav_view` / `refresh` hooks + `sessionStorage` session-ID + 250 ms throttle in `src/DashboardShell.html`; FR-60–FR-66 + AC-15–AC-17 marked **Released**; FR-65 corrected to **`LockService.getScriptLock()`** (the standalone Web App is not container-bound, so `getDocumentLock()` returns `null` and would surface as a spurious "lock timeout" warning on the first `page_load` row); version and `src/` headers → **1.6**. | Cursor |
| 2026-05-11 | 1.7 | **Whole-app branding consolidation**: `FR-50` strengthened from SHOULD/Finance-only to **MUST** across the entire app, anchored to **`agreement-dashboard-prd-v2.md` §9.5–§9.7** with the full root-variable list spelled out; `FR-10b` clarified — **Settings** at the bottom of the sidebar is now a **plain navigation-style link** (`<a>` with `.fos-nav-btn`), no outlined or pill button chrome, click handler `preventDefault()`s the `href="#"`; new **AC-18** asserting single-palette compliance and the link-style Settings affordance. Implementation: `src/DashboardShell.html` promotes `--bg / --surface / --accent / --text / --border / …` tokens to `:root`, aliases the `--ag-*` family inside `.fos-agreement-root` to those globals, retires the legacy `--harpin-navy*` / `--harpin-teal*` palette and the `.btn-fos-sidebar-footer` class, promotes the 4px custom scrollbar from the agreement scope to the whole document, rebuilds sidebar / topbar / nav / Home card / modal selectors against the new tokens, and replaces the Settings outlined pill with a `.fos-nav-btn` `<a>` (with `event.preventDefault()` on the handler); `src/NotAuthorized.html` adopts the same root tokens (deep-navy surface, 12px radius, Inter); feature **`docs/features/001-dashboard-shell-navigation.md`** updated with new design line, AC for whole-app token compliance, AC clarifying Settings link affordance, and implementation checkboxes; version and `src/` headers → **1.7**. | Cursor |
| 2026-05-12 | 1.12.0 | **Utilization Management Dashboard — Phase A live wiring.** Operations panel (`#panel-operations`, route id `operations`) flipped from the "coming soon" placeholder to the Utilization Management Dashboard. Adds new server modules `src/utilizationThresholds.js` (Script-Property-overridable defaults: `UTILIZATION_CACHE_TTL_MINUTES` default 10, `UTILIZATION_DEFAULT_RANGE_DAYS` default 90, `UTILIZATION_MAX_RANGE_DAYS` default 365, `UTILIZATION_WEEKLY_CAPACITY_HOURS` default 40, `UTILIZATION_TARGET_PERCENT` default 85, `UTILIZATION_UNDER_PERCENT` default 60, `UTILIZATION_OVER_PERCENT` default 110, `UTILIZATION_INTERNAL_COMPANY_NAMES` CSV default `harpin.ai,Harpin`, `UTILIZATION_TOP_N_PERSONS`/`PROJECTS`/`CUSTOMERS` default 20) and `src/fiberyUtilizationDashboard.js` (paginated `Agreement Management/Labor Costs` fetch with `q/order-by` wrapped-vector form per the v1.9.2 fix, row normalization with `hours` coercion / ISO-Monday week derivation / `isPending` + `isInternal` predicates / `revenueFromLabor`, KPI math §U.1–§U.7 incl. honest Effective Bill Rate coverage label, server-side `byCustomer` / `byProject` / `byPerson` / `byRole` / `byWeek` / `billableVsNonBillable` aggregates, deterministic customer + role color maps, range defaulting + clamping against `UTILIZATION_MAX_RANGE_DAYS`, `getUtilizationCacheTtlMinutes()`, and `_diag_pingUtilization` / `_diag_sampleUtilizationPayload` editor helpers). Cache schema **v1** (`fos_utilization_dashboard_v1`); TTL preference key **`fos_utilization_dashboard_ttl_minutes_v1`**. Client (`src/DashboardShell.html`): new `#panel-operations` markup with header / refresh-row / filter bar / 6-card KPI strip / four Chart.js canvases (Customer bar, Project bar, Weekly trend line, Billable-vs-Non-billable stack), nav-click branch routes `operations` to `showOperations()` (no more coming-soon modal), filter state model (customers + projects multi-select, Billable segmented toggle, Internal-labor segmented toggle, range preset / Custom), in-memory `applyFilters` + `computeKpisClient` + `groupHoursBy` + `aggregateByWeek` helpers so filter changes re-aggregate without a server roundtrip, chip bar with `×` per active filter and a Clear filters affordance, click-to-toggle on Customer + Project bars and on Billable-stack segments, activity logging under new event types `util_filter` / `util_drill` / `util_range_change` with `Route = operations` (and the existing `refresh` / `ttl_change` / `nav_view` types). New **§3.9** (FR-70–FR-76), new **§8** (Utilization Management Dashboard; renumbered the prior §8 / §9 / §10 / §11 → §9 / §10 / §11 / §12), new **AC-22** (panel activation + cache + TTL + Stale badge) and **AC-23** (filter / drill / honest Effective Bill Rate). FR-10a updated to flag the live Operations route. **§3 Overview** updated to mention the Phase A delivery. MINOR bump per `.cursor/rules/google-apps-script-core.mdc` — net new user-visible behavior + new server modules. Version and `src/` headers → **1.12.0**. | Cursor |
| 2026-05-11 | 1.11.0 | **Internal route id alignment: `finance` → `agreement-dashboard`.** Completes the Finance-rename work started in v1.10.1 by flipping the internal identifiers so the route id matches the user-facing label. Client (`src/DashboardShell.html`): nav-icon map key, panel DOM id (`#panel-finance` → `#panel-agreement-dashboard`), scoped CSS selector, JS variable (`els.panelFinance` → `els.panelAgreementDashboard`), function (`showFinance` → `showAgreementDashboard`), and nav-click route check all updated. Activity-log emissions now tag **`Route = agreement-dashboard`** instead of `finance`, and the financial-table tab event type was renamed `finance_table_tab` → **`agreement_table_tab`**. Server (`src/Code.js`): `buildNavigationModel_` nav item id flipped to `'agreement-dashboard'`. `sessionStorage` (`fos_agreement_dashboard_v2`) and `localStorage` (`fos_agreement_dashboard_ttl_minutes_v1`) keys were already aligned in earlier releases and stay untouched, so existing user caches keep working. Historical `User Activity` rows tagged `Route = finance` and `Event Type = finance_table_tab` remain queryable alongside new rows. PRD wording updated in **§2 Summary**, **§3 Overview**, **FR-10a**, **FR-52**, **§7 intro**, **§7.1 architecture note**, **AC-14**, and **AC-16**. `docs/features/001 / 003 / 004` route-id references flipped to the new value. README "Agreement Dashboard" row note updated. MINOR bump per `.cursor/rules/google-apps-script-core.mdc` — documented contract (FR-10a / AC-14 / AC-16 route id) changed. Version and `src/` headers → **1.11.0**. | Cursor |
| 2026-05-11 | 1.10.1 | **UI copy: left-nav label "Finance" → "Agreement Dashboard".** The user-facing nav entry in the sidebar and the topbar title are now **Agreement Dashboard**. Server label flipped in `buildNavigationModel_` (`src/Code.js`); client topbar title flipped in `showFinance()` (`src/DashboardShell.html`). The internal **route id remains `finance`** to preserve activity-log continuity (existing `nav_view` / `refresh` / `ttl_change` / `finance_table_tab` rows stay queryable alongside new ones), keep the `panel-finance` DOM id stable, and avoid `sessionStorage` / `localStorage` cache-key churn. PRD wording updated across **§2 Summary**, **§3 Overview**, **§4 Stakeholders**, **FR-10a** (route-id → label map), **FR-50**, **FR-52**, **FR-56 / FR-56b**, **§7 intro**, **§7.2**, **§7.3**, and **AC-14 / AC-16 / AC-18 / AC-19 / AC-20 / AC-21**. `docs/features/003-…` Status/Goal/User Stories updated to the new label. README "Finance" row retitled with a note about the preserved route id. PATCH bump per `.cursor/rules/google-apps-script-core.mdc` — no documented behavior changed. Version and `src/` headers → **1.10.1**. | Cursor |
| 2026-05-11 | 1.10.0 | **Agreement / Finance dashboard — Phase B + Phase C delivered.** Closes the in-scope §7 surface from `docs/agreement-dashboard-prd-v2.md`. Adds **Customer Relationship Cards** (§7.6 — initials avatar in §8.5 customer palette, agreement count, funnel stage, segment, NDA badge, formatted TCV or `Internal`; sorted by TCV desc with internal companies pushed to the bottom) and the **Forward Revenue Pipeline** (§7.8 — §5.5 monthly rate per non-Internal agreement with future revenue items, rendered as a horizontal bar list with the customer palette color; unscheduled / partially-scheduled / no-pipeline rows at reduced opacity with dashed border and inline note; sorted by monthly rate desc). Adds the **Revenue Flow Sankey** (§7.11 — three node layers Status → Customer → Type, node colors from §8.2/§8.5/§8.3, links as source→target color gradients at configurable opacity, height `max(400, node_count × 28)` capped at 900px, internal-type exclusion controlled by Script Property). Server: new `buildCustomerCards_`, `buildForwardPipeline_`, `buildSankey_` in `src/fiberyAgreementDashboard.js`; two new Script Properties wired through `src/agreementThresholds.js` (`AGREEMENT_SANKEY_LINK_OPACITY` default 0.35, `AGREEMENT_SANKEY_INCLUDE_INTERNAL` default false). Client: new CSS for the three surfaces, new markup blocks, render functions `renderCustomerCards` / `renderForwardPipeline` / `renderSankey`, plus a generic `injectScript_` helper and `loadD3Sankey()` lazy loader (D3 v7 + d3-sankey from jsDelivr). New **FR-56c** documents the Sankey config; new **AC-20** (Customer Cards + Forward Pipeline) and **AC-21** (Sankey). Feature spec `docs/features/003-…` Phase B + Phase C statuses flipped to **Delivered v1.10.0**. Version and `src/` headers → **1.10.0**. | Cursor |
| 2026-05-11 | 1.9.2 | **Fibery `q/order-by` syntax fix — second attempt (the right one).** The 1.9.1 object form (`{"field": "q/desc"}`) is what the Fibery MCP wrapper accepts, but the raw REST `/api/commands` endpoint that the Apps Script orchestrator hits rejects it with the same `Unknown order by expression {"v":"…"}` warning. The actual REST form is **`[[[field-path-segments], "q/asc"\|"q/desc"]]`** — i.e. the field path itself must be wrapped in a vector even for single-segment paths, matching the `q/where` field-path convention. All three call sites in `src/fiberyAgreementDashboard.js` (agreements / historical / future revenue items) corrected. Code comment now warns future maintainers off the bare-string and object forms. | Cursor |
| 2026-05-11 | 1.9.1 | **Fibery `q/order-by` syntax fix — first attempt (wrong form).** Replaced the array-of-tuples-with-bare-strings form (`[["field","q/desc"]]`) with the object form (`{ "field": "q/desc" }`) in all three queries built by `src/fiberyAgreementDashboard.js`. The previous form surfaced as `Fibery: command error: Unknown order by expression {"v":"…"}`. **Superseded by 1.9.2** — the object form is what the Fibery MCP accepts, but the raw REST `/api/commands` endpoint produces the same warning. First release on the new MAJOR.MINOR.PATCH versioning scheme — see the updated cursor rule (`.cursor/rules/google-apps-script-core.mdc` §"Version-number rules"). | Cursor |
| 2026-05-11 | 1.9 | **Phase A live-wiring schema corrections** (verified end-to-end against the live Fibery workspace via the Fibery MCP). Four field-path / shape mismatches were corrected in `src/fiberyAgreementDashboard.js`: (1) `Agreement Management/Duration` is a `fibery/date-range` whose sub-fields are **not** addressable via the `["Field","sub-field"]` form — the whole field is now selected as `duration` and unpacked to `durStart` / `durEnd` during normalization from the `{start, end}` payload (the Fibery error was `"fibery/end" field was not found in "fibery/date-range" database.`); (2) Revenue Item's customer relation is `Agreement Management/Agreement Customer` (lookup through the parent agreement), **not** `Agreement Management/Customer` — both the historical and future revenue-item queries were corrected; (3) Fibery returns `Current Margin` and `Target Margin` as decimal **fractions** (0–1) rather than percent units, so a `scaleFractionToPercent_` helper now multiplies during normalization so the §6/§8 thresholds (35, 60, …) remain in percent terms across alerts, KPIs, the financial-table margin bar, and §8.4 bucket colors; (4) `Agreement Management/Segment` is a **multi-select** that returns an array — `normalizeCompanies_` now preserves a `segments[]` array (with the joined string surfaced as `segment` for display), and `isInternalCompany_` (`src/agreementThresholds.js`) checks each entry. No PRD section / FR text changes; this is a correctness patch to the v1.8 implementation. Version and `src/` headers → **1.9**. | Cursor |
| 2026-05-11 | 1.8 | **Agreement / Finance dashboard — Phase A live wiring**: `FR-52`–`FR-56` flipped to **Released** and rewritten to reflect the Fibery REST **`/api/commands`** transport; new `FR-56a` (§8 thresholds/colors as code constants with Script Property overrides) and `FR-56b` (configurable client-side TTL — 5 / 10 / 30 / Off, default 10 min, persisted in `localStorage`, with **Stale** badge and background refresh-on-open semantics); `AC-14` rewritten for the live wiring; new **AC-19** for the Phase A charts/table/attention-panel rendering and TTL behavior. Implementation: new server modules **`src/fiberyClient.js`** (single batched `UrlFetchApp` wrapper, error mapping, no payload logging), **`src/agreementThresholds.js`** (§8.1–§8.6 constants + Script Property overrides), **`src/agreementAlerts.js`** (§6.1–§6.7 rule evaluator), and rewritten **`src/fiberyAgreementDashboard.js`** (4 queries, normalization, enrichment with scheduling status + revenue-item counts, KPIs, chart view models, financial table builder, `getAgreementCacheTtlMinutes()`, and the `_diag_pingFibery` / `_diag_sampleAgreementPayload` editor helpers); cache schema **bumped to v2** (key `fos_agreement_dashboard_v2`); `src/DashboardShell.html` adds an **Auto-refresh** selector, **Stale** badge, an Attention Items panel, four Chart.js canvases (lazy-loaded from jsDelivr CDN), and the Financial Performance tabbed table (All active / Top customer / Other customers); feature **`docs/features/003-agreement-dashboard-fibery-client-cache.md`** updated with Phase A delivered + Phase B/C scoped + Script Property reference; version and `src/` headers → **1.8**. | Cursor |
